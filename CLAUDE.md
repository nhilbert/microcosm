# CLAUDE.md — Microcosm

A mobile-first ecosystem sandbox: a single-file React artifact in which artificial organisms live, eat, reproduce, and die inside a conserved-matter world. The purpose is **exploration and play** — build a genuinely complex living system, give the player sensible levers, and report honestly on its status, development, and the impact of interventions. Modeling plus monitoring. A Kotlin/Android port is the eventual target; `core.js` plus the harnesses are the conformance spec for it.

## Files

- `src/sim/` — the simulation: `params.js` (PRNG + constants), `species.json` (**the species table — row order is the species index, part of the RNG contract**; inlined by build.py; schema + design notes in docs/species-schema.md), `traits.js` (defaults, loader, locus guardrail, `SPECIES` registry — use it instead of literal species indices), `world.js` (SoA state, spawn/kill), `events.js` (the only legal outside mutation), `fields.js` (diffusion, light, spatial hash), `step.js` (**the RNG-order contract + the tick**), `init.js` (setup + Node exports). Pure, deterministic, UI-free. The precious thing.
- `src/observatory/` — `recorder.js`, `analysis.js`, `impact.js`, `levels.js` (Phase 8 learning levels: definitions as data + pure-observer verdicts; setup composes only initWorld scenarios and events). Pure observers: **zero PRNG draws**, no mutation of dynamic state. Free to rewrite in a port; the sim is not.
- `src/header.jsx`, `src/ui-render.js` (palette, sprites, tint, **the whole frame pipeline**: layers, organisms, corpses, affordances — the visual grammar lives here), `src/ui-layout.js`, `src/ui-data.jsx`, `src/ui-reset.jsx`, `src/ui.jsx` (component, gestures, selection, actions, panels), `src/ui-levels.jsx` (the app shell: start screen, experiment HUD, verdicts, session badges — the default export since Phase 8) — UI layers; `tools/build.py` concatenates all into `dist/microcosm.jsx`, the deliverable artifact. Generated and committed; never hand-edit, CI enforces that it matches `src/`.
- `harness/tune2.js` — 8-seed × 18,000-tick ecology harness (seeds 11,22,33,44,55,66,77,88). The acceptance authority. Exits non-zero if any seed aborts.
- `harness/conform.js` — fast conformance check (seeds 11+88, t=3,000 fingerprint). **Hash-bound**: the baseline stores a sha256 of the built `dist/core.js`; a hash mismatch with identical fingerprint = behavior-neutral edit, changed fingerprint = undeclared behavior change. `--capture` recaptures (only with a declared reason).
- `harness/k6gate.js` — Phase 4 gate script (the Observatory must narrate the decomposers-off collapse unprompted; healthy control silent). Exits non-zero if the gate fails.
- `docs/porting.md` — the contract for an Android or compiled-language port: what must be bit-exact, what may be rewritten, and how a port proves itself.
- `harness/light.js` — Phase 7 L measurement and gate (`--viability`, `--patches [--seed]`, `--gate`); `npm run light`, `npm run light:gate`.
- `harness/lib.js` — shared primitives every harness and experiment builds on (`pops`, `auditM`, `locusStats`, `start`, `pin`, `coreCollapsed`, cycle estimators). `MC_CORE=<path>` points it at another build (historical cores, ports). `harness/selftest.js` checks the estimators on synthetic series — it runs inside `npm test`, and it caught a period estimator that returned a quarter period.
- `harness/levels.js` — the Phase 8 honesty gate (`npm run levels`, in `test:full`): every shipped level must FAIL untouched, PASS on its taught strategy, and FAIL on a plausible wrong lever. A level that cannot clear all three is a demonstration, not a challenge, and does not ship.
- `harness/yoshida-baseline.json` — seed-22 cycle metrics written by `npm run yoshida:capture`; `gate5` reproduces them bit-exactly. Recapture with every declared evolving change, same discipline as the conformance baseline.
- `harness/conform-baseline.json` — the certified fingerprint plus the `coreHash` it is bound to. Never hand-edit; only `npm run conform:capture` writes it.
- `rust/microcosm-core/` — **the Rust port of the core** (sim + observatory), bit-exact to the JS reference: `math/` (self-contained, matched to V8 12.4 — never call `f64::sin` here), `step.rs` (carries the RNG-order banner verbatim), `observatory.rs`, `snapshot.rs` (save/load), `wasm.rs` (the C ABI). `rust/wasm/core.js` presents it as `dist/core.js`, so `MC_CORE=rust/wasm/core.js` points any harness at the ported core. `species_gen.rs` is GENERATED from the built JS core by `tools/gen-species-rs.js` — never hand-edit, CI enforces it. Proof commands: `npm run port:check` (world + events + scenario), `npm run port:math` (needs a trace from `dev/xcheck/gen.js`), `npm run port:snapshot`. Plan and measured status: docs/android-port-plan.md.
- `LICENSE` (GPL-3.0-or-later, code) and `LICENSE-docs` (CC BY-SA 4.0, everything in `docs/`). The GPL notice lives at the top of `src/header.jsx` so the built artifact carries it once; the sim sources deliberately carry no per-file header, to keep the core hash stable.
- `docs/` — concept, phase plans, architecture review, observatory design, genetics research. Closure records and calibration histories live there; read them before touching related systems.

## Non-negotiable working rules (all earned the hard way)

1. **RNG-order contract** (banner comment atop `src/sim/step.js`): every organism's random draws happen in fixed order; never add, remove, or reorder draws in step()-reachable code without declaring an ecology change plus full re-acceptance. Use the pre-draw pattern for new randomness.
2. **Conformance ritual**: after every core edit, run `npm run conform`. Observer/instrumentation changes must be **bit-identical**. Behavior changes must be declared, re-accepted via tune2, and re-captured. Never trust a green check without the hash note making sense. **A handoff or phase closure is not complete while `conform.js` prints a NOTE** — a stale hash means the baseline no longer certifies the file that actually produces it. Rebind it with a declared reason; recapturing is always a visible, deliberate act, never a way past a warning. Fingerprint identity is the behavioral claim, so recapturing on a *changed* fingerprint would launder a behavior change — the one thing never to do.
3. **Per-edit verification**: patch via exact-string replacement with occurrence-count checks (`count==1`), a fails list, and halt-on-mismatch. Check the file, not the memory of the file — grep anchors before patching.
4. **Instrument before knob**: measure before tuning. Every calibration in this project that started from theory failed against measurement (see the calibration histories in observatory-design.md).
5. **Determinism**: no `Date.now()` / `Math.random()` in core mechanisms; sim-tick timestamps only. UI-side randomness for reset seeds is fine.
6. **Honesty over polish**: impact wording is "since", never "because"; warnings are calibrated against measured reference bands; failures and incidents get recorded, not smoothed over.
7. **Color grammar**: amber (#F2B24A) marks the player's hand, exclusively. Species colors belong to the world. Venator identity: glacier blue [168,214,244].
8. **Naming**: functional first, science as subtitle ("Recycling speed — mineral turnover").
9. **Scope**: one extension, then defer. The deferral list with re-entry conditions is in phase3-plan.md.
10. **Build check**: `npm test` (build + syntax + conformance) after any edit; it is the same gate CI runs. Artifact constraints: single file, React only, **no localStorage**. Scripts live in `package.json` — prefer them over raw invocations so CI and local runs cannot drift.

11. **The port freeze (2026-08-31, while the migration is in flight)**: `src/sim/` is feature-frozen. An ecology change landed only in JS silently forks the world from `rust/microcosm-core`, which is the one thing the port exists to prevent — the whole point is to stop maintaining two copies. UI, levels and harness work continue as normal. The freeze lifts at M4 (handover), after which the crate is the spec and changes land in Rust. Node is pinned to 22: the fingerprints were captured on V8 12.4, and Node >= 23 silently changed `Math.pow`.

## Working style

Small increments, each confirmed before the next. State contradictions and trade-offs plainly; ask 1–3 questions when a decision is genuinely ambiguous rather than guessing. Take devil's-advocate positions unprompted — an unchallenged design decision in this codebase has usually been wrong.

## Current status

- **Phases 1–5 closed** with records; Phase 5 closure (all numbers) at the end of phase5-record.md. The Phase 4 gate passed all 5 criteria: the Observatory narrates the K6 strangulation unprompted (flow warning 7 min ahead, lock warning, strain CRITICAL 712 s lead, extinctions in correct ecological order), while the healthy control stays silent (2.6% vitals false-critical).
- World: 5 live species (Solara, Drifta, Cilio, Bacillus, Venator sp6), M conserved 9,011 plus recorded additions. Levers: sun drag, sun-intensity press (0.4–1.6×), mineral pour, feed/kill, species seeding (long-press picker) — all undoable and logged. Data mode: 5 pages (Populations log-scale, Chemistry stacked-conserved, Metabolism, Health vitals with measured REFERENCE_BANDS, Events feed with impact cards).
- **Integrity incident — ROOT CAUSE FOUND (Phase 5.0, 2026-08-29)**: the Phase 5 locus was live (`sigma: 0.03`) inside the certified world, undeclared. Silenced (`P.mutation=false`), the K6 gate reproduces the Phase 4 record to the second. Conformance now carries `silent` + `evolving` fingerprints; `k6gate.js` runs on the reference world and **PASSES**. The earlier decision (B) stands for the *evolving* world's ecology criterion.
- **Phase 5 heredity (shipped, `P.mutation=true`)**: Drifta locus g — escape.p + 0.22(g−0.5), kp×(1+0.25(0.5−g)), U(±0.03) kernel, corridor [0,1]. Measured: selected to the defense rail on 8/8 (0.50→0.93 by 36k); corridor CERTIFIED at both rails; sweep narrated 8/8, control silent; Yoshida antiphase prediction NOT reproduced (sweep, not polymorphism); kpSlope 0.5 gives a balanced polymorphism — **owner decision pending** whether to adopt it as a declared ecology change. Harnesses: `npm run yoshida | corridor | gate5`.
- **Open finding (untested)**: apex-loss and apex-holding worlds separate cleanly at t=18,000 — Solara 1669–1736 vs 1347–1624, Drifta 332–361 vs 456–1116, non-overlapping in both. Consistent with a trophic cascade. n=3 vs n=5 at a single time point proves no mechanism; the honest test is a same-seed A/B with Venator seeded vs not. Do not state this as a result until that runs.
- **5.6 — Cilio pursuit locus** (catchSlope 0.4): at kbSlope 0.3 no arms race (Cilio drifted thriftier, Drifta swept). **5.7 — prices by measurement (owner decision)**: `kpSlope 0.5`, `kbSlope 0.15` — **the world is balanced**: Drifta 0.39–0.60, Cilio 0.42–0.52 at 18k, sd 0.10–0.17, sweeps in both directions on 4/8. Third heredity detector **diversifying** ("tougher and faster-growing lines coexist, neither winning"); gate criterion 1 = sweep *or* diversifying ≥ 7/8. Corridor certified all 4 corners. Yoshida (corrected estimator — the first one returned quarter periods; `selftest.js` guards it now): evolution **lengthens** the cycle in every world (3340 → 4100–5300), lag moves toward zero not antiphase, no cryptic regime; the sweeping 5.5 world often erases the cycle. Corrected tables at the end of phase5-record.md. Silent 54k control: apex held 6/8 → late apex loss under evolution is attributable. Records in phase5-record.md.
- **5.8 Solara light** (lightSlope 0.5, nearly neutral — mat fitness is crowding/mineral-gated; recorded) and **5.9 Bacillus rate–yield** (rateSlope 0.5, effSlope 0.15) shipped: **four loci, all balanced** (0.39–0.62 at 18k), "diversifying" narrated per species. `curve` machinery in every expression (default 0, bit-identical); measured: curvature makes equilibria price-robust but narrows variation and suppresses the Drifta splitting the owner values — so it ships as a **Phase 6 slider**, not a default. Corridor certification now `--rails/--fuzz/--sample` (corners retired above L=3). Harness lib + selftest (caught a quarter-period estimator; Yoshida records corrected — evolution *lengthens* the cycle).
- **Phase 6 shipped (6.0–6.3)**: Evolution panel in Intervene — mutation on/off, per-locus mutation rate and curvature, price sliders with measured balance marks, presets (Shipped/Settled/Wild/Frozen) — every control an event (`mutation`, `locus`), logged, undoable, impact-carded; `rail` event + Adaptability vital. Plan + record in phase6-plan.md.
- **Corridor decision (owner, 2026-08-29)**: the one-in-216 interior collapse (seed 33, Bacillus 0.79 with keener grazers and tougher prey) is **accepted as an in-corridor finding**; rails 80/80 and fuzz 8/8 are the operative guarantee. Bacillus's corridor stays [0,1]. Record in phase5-record.md §5.9.
- **Phase 7 L — light patches (shipped, 2026-08-29)**: `W.suns` (1–4 light sources, summed toroidal Gaussians), nearest-sun phototaxis (bit-identical with one sun), sun events (`sun{k}`, `sunAdd`, `sunRemove`, `sunSet`), sun card with intensity/spread/layouts/light budget, `adapt` detector + patch marks on the Traits page. **Measured**: moving or shrinking the shipped sun collapses the core (5/8) → layouts are additive; an added sun is inert until seeded (no long-range dispersal); a dim sun below I≈0.6 grows no mat; **Solara's light locus does not adapt locally** (it reads shaded light — structural, not a price); **patches select on predation instead**: Drifta's defense and Bacillus's metabolism loci separate between patches because the grazer does not cross dark water; gate 6/8 narrated, control silent 8/8. Plan + record: docs/phase7-light-plan.md. Next blocks in owner order: heat (with gradient-field movement for light and heat, separate layer toggles), walls, movement genome.
- **Phase 7 H — heat (H.0–H.2 shipped, 2026-08-29)**: suns became **energy sources** `W.sources {x,y,i,a,sigma}` (light + warmth, negative = cold); `W.temp` + per-cell Q10 tables (maintenance 2.5, photosynthesis 1.6, decomposition 2.0, handling 0.65, pursuit 1.3), per-species `topt/ctmax` with a falling limb on gains; one gradient primitive (`W.tgx/tgy`) with thermotaxis for Drifta (0.6) and Bacillus (0.25) — hunters deliberately blind (a set-point walked the grazer away from its prey: core lost 3/8). All exactly neutral at dT=0 (bit-identical). **Measured** (`harness/heat.js --spot|--heater|--press [--nothermo] [--blind 2,6]`): hot sun halves the mat and doubles the plankton; uniform +6 loses the apex first and the decomposer second and *destabilises* the cycle; a far heater becomes a plankton monoculture with detritus piling up (tolerance never binds — budgets do); thermotaxis moves the plankton out and lets the mat back in. Records: docs/phase7-heat-plan.md §10. **H.3 shipped (declared behaviour change, owner-accepted)**: Drifta climbs the local light gradient (`W.lgx/lgy`) instead of steering to the nearest sun — plankton packs ~20% tighter around the sun, apex holds 4/8 (was 3/8); re-accepted tune2 8/8, K6, gate5 + Yoshida recapture (4,860 → 6,120), light gate 7/8, corridor rails 64/64 + fuzz 8/8, two extreme-corner grazer extinctions documented (trophic cascade at genome extremes). Open: thermal locus after multi-locus.
- **Phase 7 H.4 — closed (2026-08-30)**: grazer **attack-rate Q10 1.8** (bite scales with warmth, flatter than upkeep; exactly 1 at dT=0, fingerprints identical) — the press's grazer squeeze was the missing rate's artefact (no more near-collapse seed, CV 0.41→0.38 vs control 0.25, apex-first preserved 8/8); **heat narration** (channels 65–74 warm-core census + three calibrated detectors: thinning, pile-up, starving pack) with `npm run heat:gate` **ALL PASS** (hot sun: pile-up 8/8 at ~25 min lead, Cil+Bac thinning 8/8; press: starve 8/8 always ahead of the apex extinction; control silent, channels exactly 0). **H-P5**: Drifta separates by heat patch 7/8 (predation-mediated, as Block L); Bacillus unmeasurable — its budget excludes it from the warm patch before selection can act (overturned, recorded). **Thermal trap (CLOSED 2026-08-30, decision (a))**: the hot sun (+8) loses the core 8/8 (attributed by A/B to Drifta's H.2 thermotaxis set-point +9, NOT the attack Q10); the owner accepted it as a narrated finding — the sun stays lethal, the set-point belongs to the movement genome. Record: phase7-heat-plan.md §12. Stabilization pass at this commit: conform (no NOTE), tune2 8/8, K6 gate ALL PASS, gate5 ALL PASS (Yoshida bit-exact), heat gate ALL PASS, light gate 7/8 + control silent, corridor rails 64/64 + fuzz 8/8 with the two H.3 extreme-corner grazer extinctions reproduced tick-exact (t=7,088 / t=2,425 — the harness exits 1 on them by design; they remain the owner-accepted in-corridor finding). Ready for the next blocks: walls, multi-locus, movement genome.
- **Phase 7 M — multi-locus + thermal locus (shipped, 2026-08-30)**: `W.g` is MAXLOCI (4) planes (`W.g[k·MAXN+i]`, plane 0 = the display locus so every legacy read keeps meaning); species rows carry `loci:[...]` beyond `locus`; expression = one factor per locus per site, heredity = one draw per locus in order (**banner rule 5**). Substrate + per-locus observatory (channels 75–88, detectors per (species, locus), Traits bands, Evolution rows) shipped bit-identical. **H.5 thermal locus** (Drifta + Bacillus, declared change): Padfield down-regulation — upkeep × (1 − 0.4·(g−g0)·dT/10), priced by the warmth-scaled gain × (1 − 0.25·(g−g0)·dT/10); exactly 1 at dT ≤ 0, so the locus is **warmth-gated** and the selection-story detectors stay silent in an unwarmed world (drift is not narrated as a story — measured: drift sd would cross the diversifying threshold by ~10k). Measured (heat.js --thermal): heater patch → Drifta adapts locally through its own expression (adapt 5/8; the defense locus needed predation, 7.L); hot sun +8 → **evolution does not outrun the thermal trap** (core lost 8/8 unchanged; §12.2 closed: the sun stays lethal, owner decision (a)); Bacillus stays budget-excluded from the warm core at warmSlope up to 0.6 (H-P5 price-robust). Re-accepted: tune2 8/8, K6 ALL PASS (record to the second), gate5 ALL PASS + Yoshida recapture (4,280 on), heat gate ALL PASS, light gate control-silent (adapt 4/8 — stream-sensitive, threshold-straddling, recorded), corridor rails+fuzz per (species, locus). Records: docs/phase7-multilocus-plan.md. Light gate 4/8 and the thermal prices (Bacillus warm-core exclusion) owner-accepted as findings. **Locus visual grammar (owner decision, 2026-08-30 — IMPLEMENTED 2026-08-30 with the movement-genome UI block)**: tint = temperature loci (heat-tolerant leans warm; species without one render base colour); ring outline = defense (Drifta); corner roundness circular↔square = feeding/metabolic axes (Cilio, Bacillus; thrifty rounds, keen sharp); elongated↔circular reserved for a future speed locus; warmth-preference locus has no body channel (D7 — behaviour is its display). Documented exceptions: the mat carpet keeps its light-locus turn (pixel field, no form channel); Traits histograms keep generic tint as chart colour. Record: phase7-multilocus-plan.md decision 2.
- **Phase 7 W — walls (shipped, 2026-08-30, owner request)**: thin FACE barriers (corner-snapped staircases, `W.walls` → per-face planes) with four properties — light/warmth transmission (per-source DDA occlusion, feeds `lgx/tgx` gradients automatically), flow (flux-pair diffusion factor: M, scent, alarm — conservation exact by construction), passage (per-species bitmask). Movement is one axis-separated sliding mover (`moveOrg`, drift/tumble/steer/jink); hunters skip prey beyond faces they cannot cross; blocked dispersal settles the child beside the parent. `wallAdd {x0,y0,dx,dy,...}` (drag VECTOR, not endpoints — minimal-image flips long strokes), `wallRemove`, `wallSet`; all undoable, impact-carded as presses. UI: long-press picker "▦ Wall" / `w`, amber preview, slate polylines (dashed = permeable, faded = glass), `W.wShade` shadow overlay keeps the painted glow honest, wall card with Stone/Glass/Fine-mesh/Coarse-mesh presets + passage chips. **Draw-free everywhere; banner rule 6**: no walls ⇒ certified arithmetic bit for bit (conform identical, tune2 8/8, `walls.js --open` exact). **Measured** (plan §7a): sealed torus split → audit flat 4/4, dark side strangles (K6), apex lost on the shrunken sun side 4/4; opaque wall → mat 0% in shadow sector 4/4; mesh hideout → refuge floor up 7/8, grazers excluded 5/8, and on the 3 seeds with a grazer boxed IN at founding the refuge inverts into a feedlot (Huffaker complete with failure mode); cycle damping inconsistent at this refuge size (recorded, not claimed). Plan + record: docs/phase7-walls-plan.md. Deferred: compartment analytics/audit card, refuge channels (89+), kin-interference LOS, moving walls.
- **Phase 7 MV.0 — movement observatory (shipped, 2026-08-30)**: recorder to full MAXLOCI locus planes (channels 89–116, detector slots sp*4+plane) + movement channels 117–140 per SPECIES.MOBILE (gradient alignment, slot-reuse-guarded net step, occupancy entropy, warm/ambient reserve) + `heatTrap` detector — level-based after the inside/outside gap statistic died against the +8 measurement (share saturates at 1.0, no ambient population to contrast) — firing Drifta 8/8 ahead of the core loss on every seed, Bacillus 7/8, Cilio 4/8 (~1,000–1,800-tick leads; all 19 fires ahead of the extinctions, zero behind; the grazer's collapse outruns the trend window on 4/8 — recorded), shipped world structurally silent 8/8. lib.js MSD estimators (selftest-guarded; rolling cohort after a fixed cohort starved on short lifespans) — Bacillus reads α≈1.01, textbook diffusion, the instrument's own validation. Reference bands measured (record §6 of phase7-movement-plan.md). **D5 resolved: no fix** — Bacillus realized step ≡ T.speed (max dev 4e-5), the flat tumble charge IS realized-quadratic; distortion only under torpor (Necro, not live). Fingerprints bit-identical ×4, baseline rebound (declared), K6 ALL PASS, heat gate ALL PASS, npm test green. Next: **MV.1** — the Drifta warmth-preference locus (declared change; escape-capable rails per D3, σ 0.03 per D4, trap-escape gate).
- **Phase 7 MV.1 — warmth-preference locus (shipped 2026-08-30, owner decision A)**: Drifta's third locus, tpref = topt + 4·(g−g0) at the draw-free thermotaxis comparison (rails [7,11], σ 0.03, warmGated, reference-rail guard; heredity rule amended for bounded references). **The flagship answered**: trap escape is threshold-like in σ — 0/8 rescued at 0.03 (cool-ward sweep arrives ~1,000 ticks late), 1/8 at 0.09, 2/8 at 0.12; the sun stays lethal at shipped σ AND evolution demonstrably discovers the better controller; Bogert interaction an honest null; heater patch separates 6/8 with adapt 7/8. **Acceptance**: tune2 7/8 — seed 77's grazer loss (t=12,851) attributed by A/B to the stream re-roll (locus inert unwarmed; fresh-seed fragility 0/16 old = 0/16 new) and **owner-accepted as an intrinsic-fragility finding** (the one-in-216 precedent); all other gates green (K6, heat, light, gate5 + Yoshida recapture, corridor rails with the two known corner collapses tick-exact); conform + yoshida recaptured, no NOTE. **Instrument incident recorded**: corridor fuzz silently ran at ×1 since Phase 6 (LOCUS_SHIPPED restore ate the ×4; caught by a tick-exact fuzz≡tune2 collapse) — fixed; the criterion's first honest ×4 run: **CERTIFIED 8/8** over 54k ticks (seed 77 included; Solara's light locus rails shade-tolerant 8/8 under ×4 — "nearly neutral" isn't at that supply; apex lost 6/8, the known finding amplified). UI: grammar increment + Traits scrolling + set-point in degrees shipped alongside. Records: phase7-movement-plan.md §7. **Next: MV.2** (rover/sitter; design measurements done — persistence surface safe everywhere, syndrome cancels in α → damp-led or netStep-read, invasion machinery null-validated).
- **Phase 7 MV.2 — restlessness locus (shipped 2026-08-30, owner: accept as measured)**: Drifta's fourth locus (slot full), dampEff = damp + 0.04·(g−g0), damp-led after the syndrome cancelled in α; exactly T.damp at g0, silent bit-identical, conform + yoshida recaptured. **Verdict**: no frequency dependence at full rails (the rover/sitter hypothesis fails on the producer — the question migrates to Cilio C2); a weak contingent directional advantage to roving (settled never invades, roving takes 3/8; ×4 fuzz trends roving 7/8); per-organism phenotype illegible at standing σ (Observatory-carried per plan §1.5), while the population story is real and correctly narrated (grounded roving sweep on seed 11). **Acceptance**: tune2 8/8 (apex held 3/8 — watch item), K6/gate5/heat green, light adapt 7/8, corridor rails 16/16 with the two former corner collapses now holding (corner worlds changed), honest ×4 fuzz 7/8 — seed 77's grazer loss @22,969 under quadrupled σ owner-accepted as an in-corridor finding. Records: phase7-movement-plan.md §8. **Next: MV.3** (Bacillus tumble; D5 already resolved — no cost fix needed), then MV.4 (Cilio unblinding), then MV-C (§11: owner's kill-and-stay↔kill-and-move two-phase strategy locus as C2, boldness C1).
- **Phase 7 MV-C C2 — Cilio hunting-style locus (shipped 2026-08-30; owner's concept, owner order C→3→4)**: kill-and-stay ↔ kill-and-move as a fixed two-phase post-capture program (`W.pc`, 30+30 ticks) with one heritable dial (pcSpeedSlope 0.5, pcTurnSlope 0.8, σ 0.03); exactly 1 at g0, bit-identical silent. **The cleanest battery of the phase — no concessions**: tune2 8/8, K6, gate5 (Yoshida recaptured; seed 22's evolving cycle stretches to 12,000 vs 4,860 frozen — the strongest cycle transformation yet, flagged for a post-phase Yoshida look), heat, light green; **corridor CERTIFIED 168/168** (9 loci, ×4 fuzz 8/8, first zero-collapse corridor of the movement era). **Science**: first movement locus visible in the tracks (mover quartiles net-displace up to 1.6× stayers, 6/8); at 20% minority **mutual invasibility both directions** (movers 3/8 incl. a fixation, stayers a near-fixation; both succeed on the same seed) — the protected-polymorphism signature the phase hunted, recorded as weak negative frequency dependence under drift (5% test drift-drowned at N≈100 — instrument limit recorded). Record: phase7-movement-plan.md §11 C2. **Next: MV.3** (Bacillus tumble), then MV.4 (Cilio warmth unblinding).
- **Phase 7 MV.3 — Bacillus search-style locus (shipped 2026-08-30; two owner decisions from measured surfaces)**: tumble propensity × (1 − 0.4·(g−g0)) at the existing R()<pT draw, **priced kbSlope 0.1** — shipped unpriced first and smooth-running swept 7/8 with a fixation (R1 demonstrated in-world), the kb surface found 0.1 cancels / 0.2 flips; then **the first single-locus rail failure in project history** (twitchy rail, seed 11: dispersal-poor decomposer concentrates recycling → spikier blooms → grazer starves) answered by softening tumbleSlope 0.5→0.4 (owner), fuzz having held 8/8 throughout. Shipped world settles 0.47–0.55 (the 5.7 balance band), sd ~0.10, no rails; 20% invasions lean twitchy at extreme compositions (recorded); phenotype flattens post-pricing (Observatory-carried). Battery: tune2 8/8 (apex held 6/8, best of the phase), K6, gate5/heat/light ALL PASS, **corridor CERTIFIED 184/184** (10 loci), conform + yoshida recaptured, no NOTE. Record: phase7-movement-plan.md §9. **Next: MV.4** (Cilio warmth unblinding), then the walls integration (main has PR #4) and the push to main.
- **Phase 7 MV.4 — Cilio unblinded (shipped 2026-08-31; clean battery, the H.2 debt paid)**: thermo gain 0.25 with a heritable set-point (third Cilio locus, tprefSpan 4, rails [3,7], σ 0.03, idle-branch, hunger overrides; Venator stays excluded). **The heater replay: the walk-off failure is gone** — core lost 0/8 in both heater layouts (H.2's fixed set-point had lost the grazer 3/8). Recorded cost: +10 heater loses the apex 8/8 (reference 5/8) — the warm-seeking grazer idles outside the cold apex's range; hot-sun thinning now narrated for three species. Battery: tune2 8/8 (apex held 6/8), K6/gate5/heat/light ALL PASS, **corridor CERTIFIED 200/200** (11 loci), conform + yoshida recaptured, no NOTE. **The phase's planned increments (MV.0–MV.4 + MV-C C2) are complete.** Record: phase7-movement-plan.md §11 C3. **Next: the walls integration** (main carries PR #4; merge, verify the certified silent fingerprints reproduce on the merged core, one combined re-acceptance, then main).
- **Phase 7 MV — movement genome (researched + planned, 2026-08-30)**: four-lens research in docs/movement-genome-research.md; plan with increments MV.0–MV.4 in docs/phase7-movement-plan.md, decisions D1–D7 **resolved** (owner, 2026-08-30): MV.0 movement observatory first (recorder 2→4 locus planes, gradient-alignment/run-length/entropy channels, trap detector, tumble-cost measurement), then MV.1 Drifta thermotaxis set-point locus (escape-capable rails ≈±2, σ 0.03 ships, sweep reported; the likely no-rescue null accepted in advance as a finding), MV.2 rover/sitter, MV.3 Bacillus tumble, MV.4 Cilio unblinding last and contingent. Per D7 movement loci carry **no visual-grammar channel** — legibility must come from behaviour itself (path shape, trails), Observatory as backstop; the elongation reservation stays recorded for a possible future speed locus only.
- **Phase 8.0 — learning levels (shipped, 2026-08-31, owner request)**: start screen (Sandbox | Experiments, all open, session badges — no localStorage by artifact rule), scenario founding `initWorld(seed, sc)` (**draw-free when absent**, conformance identical, hash rebound), `src/observatory/levels.js` (definitions as data, `levelCheck` a pure observer walking recorder samples — verdicts speed-independent), app shell `src/ui-levels.jsx`, apparatus gating via `levelAllows`/`levelPourOk`, reset-in-level = re-run the experiment. Increment 1 ships the single-producer arc, every number measured: **L1 First Light** (photosynthesis/carrying capacity; dim world stalls ≤55, lever rescue 1451), **L2 The Hungry Water** (Liebig; lever pinned at max after the first design was overturned — light cheese passed it 561>440; shipped: M0 0.4, near-pours 659 pass vs far 551 / null 480 fail), **L3 Everything Flows** (K6 as a level — level 1's own success locks 40% of mineral by t=8000; Bacillus rescue robust to timing/dose; first poor-water design overturned: seeded decomposers starve on energy-thin detritus). `harness/levels.js` gate ALL PASS (9 cases). Plan + calibration record: docs/phase8-levels-plan.md. Deferred ladder 4–10 mapped to measured findings there.
- **Phase 8.1 — ladder design (2026-08-31, owner request)**: docs/phase8-ladder-design.md — researched design principles (intrinsic integration, PhET implicit scaffolding, predict–observe–explain/productive failure, misconception-driven design, kishōtenketsu), the **gate-learnings inventory** (every gate/battery finding mapped to the level that spends it), premise probes on the merged core (levels gate 9/9 reproduced first; L4 keystone rescue probed — Cilio lifts the suppressed mat 21→322–426 then starves out in the poor water; L5 one grazer pack establishes 2/2 and restructures the core; L6 seed 101: one Venator pack establishes, **two packs collapse by t=12k** — overstocking as the wrong lever), framework increments F1–F6 (prediction step, in-level narration, post-pass twists, timeline scripts, region goals, A/B memory), full specs for **L4–L12** (capstone L12 = MV.1's trap-escape flagship played: σ is the lever, rescue needs variation before the crisis), calibration protocol, ship order L4→L5→L6→L9→L12 with cut lines. Open calibration questions pre-declared: L4 pour wrong-lever, L5 knife + paradox of enrichment, L8 cold-refuge rescue.
- **Perf pass (2026-08-31, review + increments A/B shipped)**: docs/perf-review-2026-08-31.md. A — corridor + tune2 fan runs across cores (`harness/pool.js`, fresh process per run, `MC_JOBS` caps; output byte-identical to sequential, proven on `--sample 2` and tune2 8/8). B — bit-identical core pass (trait/locus hidden-class canonicalization, sz^0.75 cached at spawn, wall-free diffusion body, hunt scan inlined), conform fingerprints identical, baseline rebound declared; walls `--open` transparent-wall proof PASS. Measured: 18k run 30.5→25.4 s, tune2 273.7→68.6 s, test:full ~2h20m→41m43s on 4 cores; all gates green after the pass. Remaining 100×-levers unchanged: more cores, and a measured native core behind `MC_CORE` when the Android port starts.
- **Phase 8.2 — ladder increment 1 (shipped 2026-08-31)**: framework **F1** (prediction step — commit-then-contrast on the start screen and Next-chaining, never graded; retrofitted to L1–L3) and **F2** (in-level Observatory narration: per-level sysEvent filter in the HUD), plus **L4 The Gardener** (competition + keystone grazing, seed 101 poor water: null 67 / mat-pours 64 — **the pours feed the bloom** — vs grazer rescue 313–388, pass window t=2000–7000 seeding, overdose forgiven, D-floor dropped after the honest rescue itself hit D=17; the twist is machinery-free — the gardener's starvation is narrated by existing events). Levels gate 13/13, conform rebound (no NOTE), UI flow verified in-browser. Record: phase8-levels-plan.md §3.
- **Phase 8.3 — L5 The Richer Pond (shipped 2026-08-31; two designs overturned by measurement first)**: "Boom and Bust" died — the apexless core **settles** after the grazer's founding crash (D 277–415 band, no cycles; the cycles lesson migrates to the full world/L6), every knife candidate was forgiven (overstock ×5, too-early, panic pours), and the **paradox of enrichment is a definitive null** (75 pours / 3,000 M → stable, C 147, S 1,971 — recorded, twist cut); the prey-floor redesign died too (trough tracks timing, not dose: 45–121 across timings). Shipped: top-down structure vs bottom-up inputs on seed 202 — 30 pours leave the meadow at 897 with the bloom fatter (D 1,851), one grazer pack lifts it to 1,660; goal S ≥ 1,250 + C ≥ 20 by t=17,000. Misconception: "more resources = more growth". Gate 17/17, conform rebound, record in phase8-levels-plan.md §3.
- **Phase 8.4 — player-language style guide (2026-08-31, owner request)**: docs/phase8-language-style.md — the target reader (casual adult, basic biology, phone, half attention), five research-backed rules (conversational per Mayer's personalization, coherence/no-ornament, plain language ≤ grade 8, name-AFTER-experience with a one-new-science-name-per-level **term ladder L1–L12**, microcopy budgets), hard per-surface budgets (avg ≤ 14 w/sentence, max 20, FK ≤ 8), banned-words list, teleology-proof grammar for the evolution levels (lines out-breed; nothing "adapts in order to"), honesty-in-prose rules ("since" not "because" for players too). **Measured audit of shipped text**: 8 texts flagged (worst: L1 reflect FK 12.0, L5 debrief pass grade 10.6, two 35-word sentences, "Paine" in body text) — rewrite queued as its own increment before L6's text is written, audit script to be promoted into harness/ then.
- **Phase 8.5 — prose pass (shipped 2026-08-31)**: `harness/prose.js` promoted into `npm test` (word budgets, 20-word sentence cap, FK ≤ 8, banned list, term ladder); its first run convicted **32 violations** across L1–L5 (the manual audit had found 8 — promoting the instrument is the point). All player text rewritten to style; gate clean; levels gate 17/17 unchanged (verdicts are text-independent). Style doc at v1.1: sentence-count caps demoted to shape guidance after the L5 worked rewrite beat them.
- **Phase 8.6 — L6 A Head Full of Hunters (shipped 2026-08-31)**: energy pyramid on seed 101 — pass = Venator ≥ 4 held 7,000 ticks by t=15,000; the pinned wrong lever (two packs at t=4,000) tops out at a 5,400-tick hold and starves out at 11,300 (margin 1,600); one pack passes at 9,700–11,700 across timings. First stateful fail predicate (`LVL.mem`, sample-driven: extinct-after-present). Recorded honestly: overstock collapse is timing-specific (two packs at t=6,000 survive — uncertified), and the t=4,000 single pack dies AFTER its pass at t=18,000, making the debrief's "one bad season from gone" literal on this seed. First level written under the enforced prose gate. Gate 21/21, conform rebound. Record: phase8-levels-plan.md §3.
- **Rust port (2026-08-31, in flight — docs/android-port-plan.md)**: owner decision — ONE Rust core (sim + observatory + its own math) compiled to Android/browser/harness/CLI, ending dual maintenance; the JS core becomes the historical oracle at handover. **Done and proven**: the math is bit-identical to V8 on all 7 functions (200k samples each, 0 mismatches); the sim reproduces the JS core bit-for-bit at 1/20/100/500/3,000/18,000 ticks on all 8 seeds, on scripted events covering every event type, and on scenario founding; `tune2` (8x18,000) is byte-identical; the observatory's 141 channels and its narration match; **K6, gate5, heat and light all pass on the ported core**. Save/load shipped early (versioned binary snapshot, 202 KB gzipped, proved by resumption). Speed x2.4-3.1 on x86-64. **M5.0 shipped and measured on a device**: a JNI probe APK (`android-native/`, rolling `probe-latest` release, built by CI) runs the core natively — a Fairphone 5 reproduces the four certified fingerprints bit-for-bit, replays the V8 math trace with 0 mismatches on all seven functions, resumes a save identically, and ticks at **0.400 ms (2,501 ticks/s, 250x speed)** — slightly faster than the x86-64 CI runner on identical work. ARM64 bit-exactness is measured, not inferred; the core sustains 250x against a UI that caps at 16x, so the bottleneck is now the renderer. **Owed**: Rust-vs-JS on the SAME phone (the ratio, as opposed to the margin, was never measured); `impact()` and the level API; M4 handover and M5.1 the Compose app. One correction is recorded in the plan: an early "K6 passes on the port" claim was false because `k6gate.js` ignored `MC_CORE` — fixed, re-run, and every other harness audited.
- **Next (owner order)**: ladder continues per phase8-ladder-design.md §7 — L9 The Sorting (first evolution level: mutation on, added grazer packs as pressure, sweep criteria as goal; wrong lever = mutation off), then L12 Outrun the Sun (re-pin an escaping seed via move.js --escape on the current core), then L7/L11/L8/L10. All player text obeys phase8-language-style.md (enforced in npm test). Also queued: post-phase Yoshida study (the stream-sensitive seed-22 cycle across builds), third loci where a question warrants them, a split-detector once a split is observed in a shipped world; Mycora/Necro re-entry conditions unchanged. Movement genome MV.0-MV.4 + MV-C complete; walls integrated 2026-08-31.
- **(was) Next: Solara light-adaptation locus** (shade-tolerant ↔ sun-loving; factor `1 + s·(g−g0)·(1−2L)` inside the photosynthesis block, priced by the light field — the sun lever becomes a selection pressure), then **Bacillus rate–yield** (voracious ↔ frugal; `rateE` up, `effE` down). Each its own declared change: expression at existing draw sites only; `silent` fingerprint identical; price by measured surface before shipping; re-accept with tune2 + corridor (16 corners) + gate5. Then multi-locus (`W.g[k*MAXN+i]`, per-locus channels, shape/outline encoding). Venator stays excluded (decision 3).

## Brief history (details in docs/)

P1 built the living pond and the observation UI. P2 built conserved chemistry, corpses and detritus, and Bacillus — its crown finding was the K6 experiment (decomposers off → slow mineral strangulation), which became the ground truth for everything after; also the transport discovery (mixing feeds the core) and the M-audit discipline. P3 added Cilio flee/alarm and Venator (pack founding, after five failed establishment designs — an apex predator is knife-edged by nature; accepted with a natural-turnover criterion), deferred Necro and Mycora with named re-entry conditions, and dropped barriers when the project refocused on monitoring. P4 built the Observatory: ring-buffer recorder (pure observer, bit-identity enforced), detectors, calibrated indicators (every detector's first theory died against measurement — absolute thresholds, EWS statistics, and level-based depletion all failed; shipped: mechanistic vitals against measured reference bands, plus trend+level+destination depletion logic), levers with honest provenance and undo, five chart pages, impact cards (ITS baselines, natural-variability tiers, press-backdrop flags), and passed the narrate-the-collapse gate. Post-P4: research and plan for heredity; a tweak batch (reset, seeding, Ghost-Ray Venator — final form a single heading-aligned spearhead, wake animation cut as beside the point); and the integrity incident with its hash-binding fix.

The project's meta-lesson, worth carrying into every future phase: the instrument is only as honest as the calibration fights behind it.
