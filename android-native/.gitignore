# Generated by tools/build-native.sh — the Rust .so and the math trace asset.
# Both are build outputs: the .so comes from rust/microcosm-android, the trace from
# dev/xcheck/gen-bin.js, and CI rebuilds both for every APK.
app/src/main/jniLibs/
app/src/main/assets/
# Gradle
.gradle/
build/
app/build/
local.properties
