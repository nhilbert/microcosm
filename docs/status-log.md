# Status log — Microcosm

The project's running record, moved out of CLAUDE.md on 2026-09-02 because it
had grown to 50 KB and was loading into every session ahead of the eleven rules
it was meant to support.

Nothing here has been edited or summarised: the entries are verbatim, oldest
first, exactly as they were appended. CLAUDE.md keeps a short current-state
summary and points here. Append new entries at the end.

The phase records in this directory (`phase5-record.md`, `phase7-*.md`,
`phase8-levels-plan.md` and the rest) remain the detailed source; this is the
index that says what happened when.

---

## The record

- **Phases 1–5 closed** with records; Phase 5 closure (all numbers) at the end of phase5-record.md. The Phase 4 gate passed all 5 criteria: the Observatory narrates the K6 strangulation unprompted (flow warning 7 min ahead, lock warning, strain CRITICAL 712 s lead, extinctions in correct ecological order), while the healthy control stays silent (2.6% vitals false-critical).
- World: 5 live species (Solara, Drifta, Cilio, Bacillus, Venator sp6), M conserved 9,011 plus recorded additions. Levers: sun drag, sun-intensity press (0.4–1.6×), mineral pour, feed/kill, species seeding (long-press picker) — all undoable and logged. Data mode: 5 pages (Populations log-scale, Chemistry stacked-conserved, Metabolism, Health vitals with measured REFERENCE_BANDS, Events feed with impact cards).
- **Integrity incident — ROOT CAUSE FOUND (Phase 5.0, 2026-08-29)**: the Phase 5 locus was live (`sigma: 0.03`) inside the certified world, undeclared. Silenced (`P.mutation=false`), the K6 gate reproduces the Phase 4 record to the second. Conformance now carries `silent` + `evolving` fingerprints; `k6gate.js` runs on the reference world and **PASSES**. The earlier decision (B) stands for the *evolving* world's ecology criterion.
- **Phase 5 heredity (shipped, `P.mutation=true`)**: Drifta locus g — escape.p + 0.22(g−0.5), kp×(1+0.25(0.5−g)), U(±0.03) kernel, corridor [0,1]. Measured: selected to the defense rail on 8/8 (0.50→0.93 by 36k); corridor CERTIFIED at both rails; sweep narrated 8/8, control silent; Yoshida antiphase prediction NOT reproduced (sweep, not polymorphism); kpSlope 0.5 gives a balanced polymorphism — **owner decision pending** whether to adopt it as a declared ecology change. Harnesses: `npm run yoshida | corridor | gate5`.
- **Open finding (untested)**: apex-loss and apex-holding worlds separate cleanly at t=18,000 — Solara 1669–1736 vs 1347–1624, Drifta 332–361 vs 456–1116, non-overlapping in both. Consistent with a trophic cascade. n=3 vs n=5 at a single time point proves no mechanism; the honest test is a same-seed A/B with Venator seeded vs not. Do not state this as a result until that runs.
- **5.6 — Cilio pursuit locus** (catchSlope 0.4): at kbSlope 0.3 no arms race (Cilio drifted thriftier, Drifta swept). **5.7 — prices by measurement (owner decision)**: `kpSlope 0.5`, `kbSlope 0.15` — **the world is balanced**: Drifta 0.39–0.60, Cilio 0.42–0.52 at 18k, sd 0.10–0.17, sweeps in both directions on 4/8. Third heredity detector **diversifying** ("tougher and faster-growing lines coexist, neither winning"); gate criterion 1 = sweep *or* diversifying ≥ 7/8. Corridor certified all 4 corners. Yoshida (corrected estimator — the first one returned quarter periods; `selftest.js` guards it now): evolution **lengthens** the cycle in every world (3340 → 4100–5300), lag moves toward zero not antiphase, no cryptic regime; the sweeping 5.5 world often erases the cycle. Corrected tables at the end of phase5-record.md. Silent 54k control: apex held 6/8 → late apex loss under evolution is attributable. Records in phase5-record.md.
- **5.8 Solara light** (lightSlope 0.5, nearly neutral — mat fitness is crowding/mineral-gated; recorded) and **5.9 Bacillus rate–yield** (rateSlope 0.5, effSlope 0.15) shipped: **four loci, all balanced** (0.39–0.62 at 18k), "diversifying" narrated per species. `curve` machinery in every expression (default 0, bit-identical); measured: curvature makes equilibria price-robust but narrows variation and suppresses the Drifta splitting the owner values — so it ships as a **Phase 6 slider**, not a default. Corridor certification now `--rails/--fuzz/--sample` (corners retired above L=3). Harness lib + selftest (caught a quarter-period estimator; Yoshida records corrected — evolution *lengthens* the cycle).
- **Phase 6 shipped (6.0–6.3)**: Evolution panel in Intervene — mutation on/off, per-locus mutation rate and curvature, price sliders with measured balance marks, presets (Shipped/Settled/Wild/Frozen) — every control an event (`mutation`, `locus`), logged, undoable, impact-carded; `rail` event + Adaptability vital. Plan + record in phase6-plan.md.
- **Corridor decision (owner, 2026-08-29)**: the one-in-216 interior collapse (seed 33, Bacillus 0.79 with keener grazers and tougher prey) is **accepted as an in-corridor finding**; rails 80/80 and fuzz 8/8 are the operative guarantee. Bacillus's corridor stays [0,1]. Record in phase5-record.md §5.9.
- **Phase 7 L — light patches (shipped, 2026-08-29)**: `W.suns` (1–4 light sources, summed toroidal Gaussians), nearest-sun phototaxis (bit-identical with one sun), sun events (`sun{k}`, `sunAdd`, `sunRemove`, `sunSet`), sun card with intensity/spread/layouts/light budget, `adapt` detector + patch marks on the Traits page. **Measured**: moving or shrinking the shipped sun collapses the core (5/8) → layouts are additive; an added sun is inert until seeded (no long-range dispersal); a dim sun below I≈0.6 grows no mat; **Solara's light locus does not adapt locally** (it reads shaded light — structural, not a price); **patches select on predation instead**: Drifta's defense and Bacillus's metabolism loci separate between patches because the grazer does not cross dark water; gate 6/8 narrated, control silent 8/8. Plan + record: docs/phase7-light-plan.md. Next blocks in owner order: heat (with gradient-field movement for light and heat, separate layer toggles), walls, movement genome.
- **Phase 7 H — heat (H.0–H.2 shipped, 2026-08-29)**: suns became **energy sources** `W.sources {x,y,i,a,sigma}` (light + warmth, negative = cold); `W.temp` + per-cell Q10 tables (maintenance 2.5, photosynthesis 1.6, decomposition 2.0, handling 0.65, pursuit 1.3), per-species `topt/ctmax` with a falling limb on gains; one gradient primitive (`W.tgx/tgy`) with thermotaxis for Drifta (0.6) and Bacillus (0.25) — hunters deliberately blind (a set-point walked the grazer away from its prey: core lost 3/8). All exactly neutral at dT=0 (bit-identical). **Measured** (`harness/heat.js --spot|--heater|--press [--nothermo] [--blind 2,6]`): hot sun halves the mat and doubles the plankton; uniform +6 loses the apex first and the decomposer second and *destabilises* the cycle; a far heater becomes a plankton monoculture with detritus piling up (tolerance never binds — budgets do); thermotaxis moves the plankton out and lets the mat back in. Records: docs/phase7-heat-plan.md §10. **H.3 shipped (declared behaviour change, owner-accepted)**: Drifta climbs the local light gradient (`W.lgx/lgy`) instead of steering to the nearest sun — plankton packs ~20% tighter around the sun, apex holds 4/8 (was 3/8); re-accepted tune2 8/8, K6, gate5 + Yoshida recapture (4,860 → 6,120), light gate 7/8, corridor rails 64/64 + fuzz 8/8, two extreme-corner grazer extinctions documented (trophic cascade at genome extremes). Open: thermal locus after multi-locus.
- **Phase 7 H.4 — closed (2026-08-30)**: grazer **attack-rate Q10 1.8** (bite scales with warmth, flatter than upkeep; exactly 1 at dT=0, fingerprints identical) — the press's grazer squeeze was the missing rate's artefact (no more near-collapse seed, CV 0.41→0.38 vs control 0.25, apex-first preserved 8/8); **heat narration** (channels 65–74 warm-core census + three calibrated detectors: thinning, pile-up, starving pack) with `npm run heat:gate` **ALL PASS** (hot sun: pile-up 8/8 at ~25 min lead, Cil+Bac thinning 8/8; press: starve 8/8 always ahead of the apex extinction; control silent, channels exactly 0). **H-P5**: Drifta separates by heat patch 7/8 (predation-mediated, as Block L); Bacillus unmeasurable — its budget excludes it from the warm patch before selection can act (overturned, recorded). **Thermal trap (CLOSED 2026-08-30, decision (a))**: the hot sun (+8) loses the core 8/8 (attributed by A/B to Drifta's H.2 thermotaxis set-point +9, NOT the attack Q10); the owner accepted it as a narrated finding — the sun stays lethal, the set-point belongs to the movement genome. Record: phase7-heat-plan.md §12. Stabilization pass at this commit: conform (no NOTE), tune2 8/8, K6 gate ALL PASS, gate5 ALL PASS (Yoshida bit-exact), heat gate ALL PASS, light gate 7/8 + control silent, corridor rails 64/64 + fuzz 8/8 with the two H.3 extreme-corner grazer extinctions reproduced tick-exact (t=7,088 / t=2,425 — the harness exits 1 on them by design; they remain the owner-accepted in-corridor finding). Ready for the next blocks: walls, multi-locus, movement genome.
- **Phase 7 M — multi-locus + thermal locus (shipped, 2026-08-30)**: `W.g` is MAXLOCI (4) planes (`W.g[k·MAXN+i]`, plane 0 = the display locus so every legacy read keeps meaning); species rows carry `loci:[...]` beyond `locus`; expression = one factor per locus per site, heredity = one draw per locus in order (**banner rule 5**). Substrate + per-locus observatory (channels 75–88, detectors per (species, locus), Traits bands, Evolution rows) shipped bit-identical. **H.5 thermal locus** (Drifta + Bacillus, declared change): Padfield down-regulation — upkeep × (1 − 0.4·(g−g0)·dT/10), priced by the warmth-scaled gain × (1 − 0.25·(g−g0)·dT/10); exactly 1 at dT ≤ 0, so the locus is **warmth-gated** and the selection-story detectors stay silent in an unwarmed world (drift is not narrated as a story — measured: drift sd would cross the diversifying threshold by ~10k). Measured (heat.js --thermal): heater patch → Drifta adapts locally through its own expression (adapt 5/8; the defense locus needed predation, 7.L); hot sun +8 → **evolution does not outrun the thermal trap** (core lost 8/8 unchanged; §12.2 closed: the sun stays lethal, owner decision (a)); Bacillus stays budget-excluded from the warm core at warmSlope up to 0.6 (H-P5 price-robust). Re-accepted: tune2 8/8, K6 ALL PASS (record to the second), gate5 ALL PASS + Yoshida recapture (4,280 on), heat gate ALL PASS, light gate control-silent (adapt 4/8 — stream-sensitive, threshold-straddling, recorded), corridor rails+fuzz per (species, locus). Records: docs/phase7-multilocus-plan.md. Light gate 4/8 and the thermal prices (Bacillus warm-core exclusion) owner-accepted as findings. **Locus visual grammar (owner decision, 2026-08-30 — IMPLEMENTED 2026-08-30 with the movement-genome UI block)**: tint = temperature loci (heat-tolerant leans warm; species without one render base colour); ring outline = defense (Drifta); corner roundness circular↔square = feeding/metabolic axes (Cilio, Bacillus; thrifty rounds, keen sharp); elongated↔circular reserved for a future speed locus; warmth-preference locus has no body channel (D7 — behaviour is its display). Documented exceptions: the mat carpet keeps its light-locus turn (pixel field, no form channel); Traits histograms keep generic tint as chart colour. Record: phase7-multilocus-plan.md decision 2.
- **Phase 7 W — walls (shipped, 2026-08-30, owner request)**: thin FACE barriers (corner-snapped staircases, `W.walls` → per-face planes) with four properties — light/warmth transmission (per-source DDA occlusion, feeds `lgx/tgx` gradients automatically), flow (flux-pair diffusion factor: M, scent, alarm — conservation exact by construction), passage (per-species bitmask). Movement is one axis-separated sliding mover (`moveOrg`, drift/tumble/steer/jink); hunters skip prey beyond faces they cannot cross; blocked dispersal settles the child beside the parent. `wallAdd {x0,y0,dx,dy,...}` (drag VECTOR, not endpoints — minimal-image flips long strokes), `wallRemove`, `wallSet`; all undoable, impact-carded as presses. UI: long-press picker "▦ Wall" / `w`, amber preview, slate polylines (dashed = permeable, faded = glass), `W.wShade` shadow overlay keeps the painted glow honest, wall card with Stone/Glass/Fine-mesh/Coarse-mesh presets + passage chips. **Draw-free everywhere; banner rule 6**: no walls ⇒ certified arithmetic bit for bit (conform identical, tune2 8/8, `walls.js --open` exact). **Measured** (plan §7a): sealed torus split → audit flat 4/4, dark side strangles (K6), apex lost on the shrunken sun side 4/4; opaque wall → mat 0% in shadow sector 4/4; mesh hideout → refuge floor up 7/8, grazers excluded 5/8, and on the 3 seeds with a grazer boxed IN at founding the refuge inverts into a feedlot (Huffaker complete with failure mode); cycle damping inconsistent at this refuge size (recorded, not claimed). Plan + record: docs/phase7-walls-plan.md. Deferred: compartment analytics/audit card, refuge channels (89+), kin-interference LOS, moving walls.
- **Phase 7 MV.0 — movement observatory (shipped, 2026-08-30)**: recorder to full MAXLOCI locus planes (channels 89–116, detector slots sp*4+plane) + movement channels 117–140 per SPECIES.MOBILE (gradient alignment, slot-reuse-guarded net step, occupancy entropy, warm/ambient reserve) + `heatTrap` detector — level-based after the inside/outside gap statistic died against the +8 measurement (share saturates at 1.0, no ambient population to contrast) — firing Drifta 8/8 ahead of the core loss on every seed, Bacillus 7/8, Cilio 4/8 (~1,000–1,800-tick leads; all 19 fires ahead of the extinctions, zero behind; the grazer's collapse outruns the trend window on 4/8 — recorded), shipped world structurally silent 8/8. lib.js MSD estimators (selftest-guarded; rolling cohort after a fixed cohort starved on short lifespans) — Bacillus reads α≈1.01, textbook diffusion, the instrument's own validation. Reference bands measured (record §6 of phase7-movement-plan.md). **D5 resolved: no fix** — Bacillus realized step ≡ T.speed (max dev 4e-5), the flat tumble charge IS realized-quadratic; distortion only under torpor (Necro, not live). Fingerprints bit-identical ×4, baseline rebound (declared), K6 ALL PASS, heat gate ALL PASS, npm test green. Next: **MV.1** — the Drifta warmth-preference locus (declared change; escape-capable rails per D3, σ 0.03 per D4, trap-escape gate).
- **Phase 7 MV.1 — warmth-preference locus (shipped 2026-08-30, owner decision A)**: Drifta's third locus, tpref = topt + 4·(g−g0) at the draw-free thermotaxis comparison (rails [7,11], σ 0.03, warmGated, reference-rail guard; heredity rule amended for bounded references). **The flagship answered**: trap escape is threshold-like in σ — 0/8 rescued at 0.03 (cool-ward sweep arrives ~1,000 ticks late), 1/8 at 0.09, 2/8 at 0.12; the sun stays lethal at shipped σ AND evolution demonstrably discovers the better controller; Bogert interaction an honest null; heater patch separates 6/8 with adapt 7/8. **Acceptance**: tune2 7/8 — seed 77's grazer loss (t=12,851) attributed by A/B to the stream re-roll (locus inert unwarmed; fresh-seed fragility 0/16 old = 0/16 new) and **owner-accepted as an intrinsic-fragility finding** (the one-in-216 precedent); all other gates green (K6, heat, light, gate5 + Yoshida recapture, corridor rails with the two known corner collapses tick-exact); conform + yoshida recaptured, no NOTE. **Instrument incident recorded**: corridor fuzz silently ran at ×1 since Phase 6 (LOCUS_SHIPPED restore ate the ×4; caught by a tick-exact fuzz≡tune2 collapse) — fixed; the criterion's first honest ×4 run: **CERTIFIED 8/8** over 54k ticks (seed 77 included; Solara's light locus rails shade-tolerant 8/8 under ×4 — "nearly neutral" isn't at that supply; apex lost 6/8, the known finding amplified). UI: grammar increment + Traits scrolling + set-point in degrees shipped alongside. Records: phase7-movement-plan.md §7. **Next: MV.2** (rover/sitter; design measurements done — persistence surface safe everywhere, syndrome cancels in α → damp-led or netStep-read, invasion machinery null-validated).
- **Phase 7 MV.2 — restlessness locus (shipped 2026-08-30, owner: accept as measured)**: Drifta's fourth locus (slot full), dampEff = damp + 0.04·(g−g0), damp-led after the syndrome cancelled in α; exactly T.damp at g0, silent bit-identical, conform + yoshida recaptured. **Verdict**: no frequency dependence at full rails (the rover/sitter hypothesis fails on the producer — the question migrates to Cilio C2); a weak contingent directional advantage to roving (settled never invades, roving takes 3/8; ×4 fuzz trends roving 7/8); per-organism phenotype illegible at standing σ (Observatory-carried per plan §1.5), while the population story is real and correctly narrated (grounded roving sweep on seed 11). **Acceptance**: tune2 8/8 (apex held 3/8 — watch item), K6/gate5/heat green, light adapt 7/8, corridor rails 16/16 with the two former corner collapses now holding (corner worlds changed), honest ×4 fuzz 7/8 — seed 77's grazer loss @22,969 under quadrupled σ owner-accepted as an in-corridor finding. Records: phase7-movement-plan.md §8. **Next: MV.3** (Bacillus tumble; D5 already resolved — no cost fix needed), then MV.4 (Cilio unblinding), then MV-C (§11: owner's kill-and-stay↔kill-and-move two-phase strategy locus as C2, boldness C1).
- **Phase 7 MV-C C2 — Cilio hunting-style locus (shipped 2026-08-30; owner's concept, owner order C→3→4)**: kill-and-stay ↔ kill-and-move as a fixed two-phase post-capture program (`W.pc`, 30+30 ticks) with one heritable dial (pcSpeedSlope 0.5, pcTurnSlope 0.8, σ 0.03); exactly 1 at g0, bit-identical silent. **The cleanest battery of the phase — no concessions**: tune2 8/8, K6, gate5 (Yoshida recaptured; seed 22's evolving cycle stretches to 12,000 vs 4,860 frozen — the strongest cycle transformation yet, flagged for a post-phase Yoshida look), heat, light green; **corridor CERTIFIED 168/168** (9 loci, ×4 fuzz 8/8, first zero-collapse corridor of the movement era). **Science**: first movement locus visible in the tracks (mover quartiles net-displace up to 1.6× stayers, 6/8); at 20% minority **mutual invasibility both directions** (movers 3/8 incl. a fixation, stayers a near-fixation; both succeed on the same seed) — the protected-polymorphism signature the phase hunted, recorded as weak negative frequency dependence under drift (5% test drift-drowned at N≈100 — instrument limit recorded). Record: phase7-movement-plan.md §11 C2. **Next: MV.3** (Bacillus tumble), then MV.4 (Cilio warmth unblinding).
- **Phase 7 MV.3 — Bacillus search-style locus (shipped 2026-08-30; two owner decisions from measured surfaces)**: tumble propensity × (1 − 0.4·(g−g0)) at the existing R()<pT draw, **priced kbSlope 0.1** — shipped unpriced first and smooth-running swept 7/8 with a fixation (R1 demonstrated in-world), the kb surface found 0.1 cancels / 0.2 flips; then **the first single-locus rail failure in project history** (twitchy rail, seed 11: dispersal-poor decomposer concentrates recycling → spikier blooms → grazer starves) answered by softening tumbleSlope 0.5→0.4 (owner), fuzz having held 8/8 throughout. Shipped world settles 0.47–0.55 (the 5.7 balance band), sd ~0.10, no rails; 20% invasions lean twitchy at extreme compositions (recorded); phenotype flattens post-pricing (Observatory-carried). Battery: tune2 8/8 (apex held 6/8, best of the phase), K6, gate5/heat/light ALL PASS, **corridor CERTIFIED 184/184** (10 loci), conform + yoshida recaptured, no NOTE. Record: phase7-movement-plan.md §9. **Next: MV.4** (Cilio warmth unblinding), then the walls integration (main has PR #4) and the push to main.
- **Phase 7 MV.4 — Cilio unblinded (shipped 2026-08-31; clean battery, the H.2 debt paid)**: thermo gain 0.25 with a heritable set-point (third Cilio locus, tprefSpan 4, rails [3,7], σ 0.03, idle-branch, hunger overrides; Venator stays excluded). **The heater replay: the walk-off failure is gone** — core lost 0/8 in both heater layouts (H.2's fixed set-point had lost the grazer 3/8). Recorded cost: +10 heater loses the apex 8/8 (reference 5/8) — the warm-seeking grazer idles outside the cold apex's range; hot-sun thinning now narrated for three species. Battery: tune2 8/8 (apex held 6/8), K6/gate5/heat/light ALL PASS, **corridor CERTIFIED 200/200** (11 loci), conform + yoshida recaptured, no NOTE. **The phase's planned increments (MV.0–MV.4 + MV-C C2) are complete.** Record: phase7-movement-plan.md §11 C3. **Next: the walls integration** (main carries PR #4; merge, verify the certified silent fingerprints reproduce on the merged core, one combined re-acceptance, then main).
- **Phase 7 MV — movement genome (researched + planned, 2026-08-30)**: four-lens research in docs/movement-genome-research.md; plan with increments MV.0–MV.4 in docs/phase7-movement-plan.md, decisions D1–D7 **resolved** (owner, 2026-08-30): MV.0 movement observatory first (recorder 2→4 locus planes, gradient-alignment/run-length/entropy channels, trap detector, tumble-cost measurement), then MV.1 Drifta thermotaxis set-point locus (escape-capable rails ≈±2, σ 0.03 ships, sweep reported; the likely no-rescue null accepted in advance as a finding), MV.2 rover/sitter, MV.3 Bacillus tumble, MV.4 Cilio unblinding last and contingent. Per D7 movement loci carry **no visual-grammar channel** — legibility must come from behaviour itself (path shape, trails), Observatory as backstop; the elongation reservation stays recorded for a possible future speed locus only.
- **Phase 8.0 — learning levels (shipped, 2026-08-31, owner request)**: start screen (Sandbox | Experiments, all open, session badges — no localStorage by artifact rule), scenario founding `initWorld(seed, sc)` (**draw-free when absent**, conformance identical, hash rebound), `src/observatory/levels.js` (definitions as data, `levelCheck` a pure observer walking recorder samples — verdicts speed-independent), app shell `src/ui-levels.jsx`, apparatus gating via `levelAllows`/`levelPourOk`, reset-in-level = re-run the experiment. Increment 1 ships the single-producer arc, every number measured: **L1 First Light** (photosynthesis/carrying capacity; dim world stalls ≤55, lever rescue 1451), **L2 The Hungry Water** (Liebig; lever pinned at max after the first design was overturned — light cheese passed it 561>440; shipped: M0 0.4, near-pours 659 pass vs far 551 / null 480 fail), **L3 Everything Flows** (K6 as a level — level 1's own success locks 40% of mineral by t=8000; Bacillus rescue robust to timing/dose; first poor-water design overturned: seeded decomposers starve on energy-thin detritus). `harness/levels.js` gate ALL PASS (9 cases). Plan + calibration record: docs/phase8-levels-plan.md. Deferred ladder 4–10 mapped to measured findings there.
- **Phase 8.1 — ladder design (2026-08-31, owner request)**: docs/phase8-ladder-design.md — researched design principles (intrinsic integration, PhET implicit scaffolding, predict–observe–explain/productive failure, misconception-driven design, kishōtenketsu), the **gate-learnings inventory** (every gate/battery finding mapped to the level that spends it), premise probes on the merged core (levels gate 9/9 reproduced first; L4 keystone rescue probed — Cilio lifts the suppressed mat 21→322–426 then starves out in the poor water; L5 one grazer pack establishes 2/2 and restructures the core; L6 seed 101: one Venator pack establishes, **two packs collapse by t=12k** — overstocking as the wrong lever), framework increments F1–F6 (prediction step, in-level narration, post-pass twists, timeline scripts, region goals, A/B memory), full specs for **L4–L12** (capstone L12 = MV.1's trap-escape flagship played: σ is the lever, rescue needs variation before the crisis), calibration protocol, ship order L4→L5→L6→L9→L12 with cut lines. Open calibration questions pre-declared: L4 pour wrong-lever, L5 knife + paradox of enrichment, L8 cold-refuge rescue.
- **Perf pass (2026-08-31, review + increments A/B shipped)**: docs/perf-review-2026-08-31.md. A — corridor + tune2 fan runs across cores (`harness/pool.js`, fresh process per run, `MC_JOBS` caps; output byte-identical to sequential, proven on `--sample 2` and tune2 8/8). B — bit-identical core pass (trait/locus hidden-class canonicalization, sz^0.75 cached at spawn, wall-free diffusion body, hunt scan inlined), conform fingerprints identical, baseline rebound declared; walls `--open` transparent-wall proof PASS. Measured: 18k run 30.5→25.4 s, tune2 273.7→68.6 s, test:full ~2h20m→41m43s on 4 cores; all gates green after the pass. Remaining 100×-levers unchanged: more cores, and a measured native core behind `MC_CORE` when the Android port starts.
- **Phase 8.2 — ladder increment 1 (shipped 2026-08-31)**: framework **F1** (prediction step — commit-then-contrast on the start screen and Next-chaining, never graded; retrofitted to L1–L3) and **F2** (in-level Observatory narration: per-level sysEvent filter in the HUD), plus **L4 The Gardener** (competition + keystone grazing, seed 101 poor water: null 67 / mat-pours 64 — **the pours feed the bloom** — vs grazer rescue 313–388, pass window t=2000–7000 seeding, overdose forgiven, D-floor dropped after the honest rescue itself hit D=17; the twist is machinery-free — the gardener's starvation is narrated by existing events). Levels gate 13/13, conform rebound (no NOTE), UI flow verified in-browser. Record: phase8-levels-plan.md §3.
- **Phase 8.3 — L5 The Richer Pond (shipped 2026-08-31; two designs overturned by measurement first)**: "Boom and Bust" died — the apexless core **settles** after the grazer's founding crash (D 277–415 band, no cycles; the cycles lesson migrates to the full world/L6), every knife candidate was forgiven (overstock ×5, too-early, panic pours), and the **paradox of enrichment is a definitive null** (75 pours / 3,000 M → stable, C 147, S 1,971 — recorded, twist cut); the prey-floor redesign died too (trough tracks timing, not dose: 45–121 across timings). Shipped: top-down structure vs bottom-up inputs on seed 202 — 30 pours leave the meadow at 897 with the bloom fatter (D 1,851), one grazer pack lifts it to 1,660; goal S ≥ 1,250 + C ≥ 20 by t=17,000. Misconception: "more resources = more growth". Gate 17/17, conform rebound, record in phase8-levels-plan.md §3.
- **Phase 8.4 — player-language style guide (2026-08-31, owner request)**: docs/phase8-language-style.md — the target reader (casual adult, basic biology, phone, half attention), five research-backed rules (conversational per Mayer's personalization, coherence/no-ornament, plain language ≤ grade 8, name-AFTER-experience with a one-new-science-name-per-level **term ladder L1–L12**, microcopy budgets), hard per-surface budgets (avg ≤ 14 w/sentence, max 20, FK ≤ 8), banned-words list, teleology-proof grammar for the evolution levels (lines out-breed; nothing "adapts in order to"), honesty-in-prose rules ("since" not "because" for players too). **Measured audit of shipped text**: 8 texts flagged (worst: L1 reflect FK 12.0, L5 debrief pass grade 10.6, two 35-word sentences, "Paine" in body text) — rewrite queued as its own increment before L6's text is written, audit script to be promoted into harness/ then.
- **Phase 8.5 — prose pass (shipped 2026-08-31)**: `harness/prose.js` promoted into `npm test` (word budgets, 20-word sentence cap, FK ≤ 8, banned list, term ladder); its first run convicted **32 violations** across L1–L5 (the manual audit had found 8 — promoting the instrument is the point). All player text rewritten to style; gate clean; levels gate 17/17 unchanged (verdicts are text-independent). Style doc at v1.1: sentence-count caps demoted to shape guidance after the L5 worked rewrite beat them.
- **Phase 8.6 — L6 A Head Full of Hunters (shipped 2026-08-31)**: energy pyramid on seed 101 — pass = Venator ≥ 4 held 7,000 ticks by t=15,000; the pinned wrong lever (two packs at t=4,000) tops out at a 5,400-tick hold and starves out at 11,300 (margin 1,600); one pack passes at 9,700–11,700 across timings. First stateful fail predicate (`LVL.mem`, sample-driven: extinct-after-present). Recorded honestly: overstock collapse is timing-specific (two packs at t=6,000 survive — uncertified), and the t=4,000 single pack dies AFTER its pass at t=18,000, making the debrief's "one bad season from gone" literal on this seed. First level written under the enforced prose gate. Gate 21/21, conform rebound. Record: phase8-levels-plan.md §3.
- **Rust port (2026-08-31, in flight — docs/android-port-plan.md)**: owner decision — ONE Rust core (sim + observatory + its own math) compiled to Android/browser/harness/CLI, ending dual maintenance; the JS core becomes the historical oracle at handover. **Done and proven**: the math is bit-identical to V8 on all 7 functions (200k samples each, 0 mismatches); the sim reproduces the JS core bit-for-bit at 1/20/100/500/3,000/18,000 ticks on all 8 seeds, on scripted events covering every event type, and on scenario founding; `tune2` (8x18,000) is byte-identical; the observatory's 141 channels and its narration match; **K6, gate5, heat and light all pass on the ported core**. Save/load shipped early (versioned binary snapshot, 202 KB gzipped, proved by resumption). Speed x2.4-3.1 on x86-64. **M5.0 shipped and measured on a device**: a JNI probe APK (`android-native/`, rolling `probe-latest` release, built by CI) runs the core natively — a Fairphone 5 reproduces the four certified fingerprints bit-for-bit, replays the V8 math trace with 0 mismatches on all seven functions, resumes a save identically, and ticks at **0.400 ms (2,501 ticks/s, 250x speed)** — slightly faster than the x86-64 CI runner on identical work. ARM64 bit-exactness is measured, not inferred; the core sustains 250x against a UI that caps at 16x, so the bottleneck is now the renderer. **M4 handover done** (the crate is the simulation; `src/sim/` + `src/observatory/` are the frozen oracle; `conform-core.js` certifies the Rust sources and requires native == wasm). **Level API ported**: the level table became data in the strict sense — predicates too (`src/observatory/levels.json` + a declarative condition schema), `levels.js` the evaluator, `levels_gen.rs` generated, `levels.rs` the runtime; the JS refactor is conformance-identical and the honesty gate's 21 cases are byte-identical on both cores. **Owed**: Rust-vs-JS on the SAME phone (the ratio, as opposed to the margin, was never measured); `impact()` (it reads the UI's event log, so it lands with the app); M5.1 the Compose app. One correction is recorded in the plan: an early "K6 passes on the port" claim was false because `k6gate.js` ignored `MC_CORE` — fixed, re-run, and every other harness audited.
- **App UX — reviewed, researched, decided (2026-09-01)**: the owner played the full-parity app and named five complaints; `docs/app-ux-review.md` traces each to its cause. Two are worse than reported: **there is no reset control at all** (and `restartLevel()` compiles with no caller), and the bottom bar is nine wrap-content buttons needing ~576 dp in a 408 dp phone with **no overflow strategy**, so `save`/`exp`/`data`/`bench` are laid out at zero width — features the milestone calls shipped, off the screen. The sun grab is three faults compounding (silent priority grip, drag stolen from the camera, no touch slop) on a lever measured at **5/8 core loss**. Also: no back handling, no autosave on pause, chips at ~2 dp against a 48 dp guideline, renderer telemetry permanently in the player's eyeline, and the browser's three-detent sheet never ported. `docs/app-ux-research.md` is the five-lens research; its crown finding is that **undo here puts the lever back, not the world** — every inverse applies to the world as it now is, nothing rewinds ticks — so the app contains actions that read as reversible and are irreversible in consequence. Design rule that falls out: **modeless where undo restores the world, quasimodal where it does not**, with the sorting to be measured via `impact()`, not guessed. **Owner decisions**: the app becomes the product (the browser retires to being the frozen oracle for the *renderer*, as `src/sim/` did for the simulation — `frame.rs` and the frame gate are untouched); research before repairs; all five lenses; **the front door is the start screen** (Sandbox | Experiments, all open — PhET-style staged tabs rejected for the sandbox because a PhET tab is its own model instance and this world is one pond you keep). U.0 shipped — next entry.
- **U.0 repairs (shipped 2026-09-01)**: all seven from the review's §5, each its own commit, the layout gate green after every one. The wide rows scroll instead of squeezing (`Chrome.build` is the single decision point the app, the gate and the camera all build through; the gate learned scroll containers; **all 35 baseline lines deleted — the baseline is at zero and may not silently grow**). Reset exists (confirm-tap 2.6 s as the browser's; in-level = re-run, `restartLevel` finally has a caller; sandbox = fresh random seed). Data pages: selected state (bold + full alpha, not amber) and horizontal swipe (dependency-free `SwipePanel`, yields to the scrolling rows). The gripped sun no longer owns every drag — only a drag that *starts* on it moves it, latched at finger-down, sun screen-position published by the render thread. Back closes report/verdict/Data/grips before leaving the app. The sandbox autosaves on pause to its own file (never the manual slot; levels excluded — the snapshot carries no level runtime) and boot restores it. Renderer telemetry (z/ms/core/drawn + bench) is dev-mode only, long-press the HUD to toggle. Also this session: `ChromeScreenshotTest` — the camera (`@GraphicsMode(NATIVE)`, without which every Canvas call is a silent no-op producing pure-black PNGs) writes `build/reports/screens/*.png`; its first frames reproduced the owner's screenshot from source, its last show the repair. **Splash-crash incident (same day, root cause found)**: the U.0 APK died at the splash on the owner's phone — a child-moving loop (`while childCount > 0 … addView`) from the Chrome refactor threw "child already has a parent" in `onCreate`, through green CI, because the layout gate measures the rows Chrome *builds* and nothing executed `MainActivity`. Answer: **the boot gate** (`BootTest`) — the real `MainActivity` + the real core (HOST-built `libmicrocosm.so`, `cargo build --release` in `rust/microcosm-android`, wired via `java.library.path`) under Robolectric, running create/resume/HUD-ticks/pause/back/destroy; it reproduced the crash on its first run, the fix is one line, CI builds the host core before the gates. Recorded limits: no Surface, no GPU, no arm64. **Owner's verdict round 1 (2026-09-01)**: bar/reset/Data/autosave confirmed working on the device; back-exit lost the world (the documented autosave race — fixed: back saves the sandbox, then finishes); Data's title drew under the status bar (fixed: the overlay gets insets); **"never get to grip the sun" is OPEN** — the boot gate now fires the real gesture at the device profile and grips in Intervene, grips nothing in Observe (asserted; the checklist sent to the owner never said Intervene is required, so mode confusion is the leading hypothesis, unconfirmed). Watch item from the screenshots: Health's per-species vitals showed only Venator after ~1 min of play.
- **Layout gate (2026-09-01)**: `android-app/app/src/test/` — the first gate in this project about the *screen* rather than the world, because the bar overflow shipped through nine green CI runs. Robolectric (CI has no device), four device profiles including the Fairphone 5, checking SQUEEZED / CLIPPED / TINY (Material's 48 dp, deliberately stricter than WCAG's 24) / OVERLAP. `Chrome.kt` defines the control rows once and `MainActivity` builds from it, so the gate measures the row that ships — a test declaring its own buttons would agree with a broken app. It is a **ratchet**: `layout-baseline.txt` records today's violations and the test fails both when a new one appears and when a listed one stops occurring, so the file can only shrink and cannot shrink silently (the `conform-baseline.json` discipline). Runs in `android-app.yml`. Caveat recorded in the source: Robolectric's text metrics are its own, so a violation is real and its silence is not proof of beauty. **First measured run: 35 violations**, baseline filled from it. Two findings: the bar wants **88 dp per button** (Material's minimum) so nine need **792 dp** — worse than the review's 576 dp estimate from a 64 dp guess; and **the 800 dp tablet profile is clean**, which proves this is width and nothing deeper. The Fairphone 5 lines reproduce the owner's screenshot from source: `save`/`exp`/`data`/`bench` laid out at 0 px each, having asked for 264 px. The gate itself was wrong once first (it carried density as a number and used it only for the viewport, so it measured four screen sizes against one phone) — fixed by handing resource qualifiers to `RuntimeEnvironment.setQualifiers`, and recorded in `LayoutGate.Profile`'s doc comment.
- **A dev environment that can build Android (2026-09-01)**: `docs/android-dev-environment.md` + `tools/setup-android-env.sh`. This container has no Android SDK and its proxy refuses `dl.google.com`, so CI is the only compiler the Kotlin has met — which cost two wrong answers in one hour (a gate failure whose reason lived in an HTML report, and the density bug above). The environment adds `dl.google.com` + `maven.google.com` to a Custom allowlist and installs JDK 17 + platform 35 in a setup script; the filesystem is snapshotted afterwards so it is paid once. Deliberately NOT installed: emulator/system images (no KVM) and the NDK (~1 GB, over the 5-minute setup budget) — the APK build stays in CI, and the gate's unit tests never load the native library. The payoff is that Robolectric can render the view tree to a PNG that can actually be looked at, which partly closes the standing gap in android-app-plan.md §6. It does not provide a device: "the owner plays it" remains the only test that settles anything.
- **Lever-outrun study (2026-09-01, U.2's measured foundation)**: docs/lever-outrun-study.md — same-seed A/B, every app lever pulled at t0=3000 and undone via the core's own undo slot after 50/600/3000 ticks, 8 seeds, departure vs a true-butterfly chaos floor. **Zero collapse mismatches anywhere** (the light plan's 5/8 sun-move collapse belongs to the move that stays); undo within a minute is functionally a time machine (a pour undone at d=50 rejoins bit-exactly, 8/8); the one lever that outruns its undo is the **sun intensity press left standing five minutes** (med 3.23 vs floor max 2.74). U.2 consequence: no lever earns arming-friction on irreversibility grounds — the sun's quasimodal case is accident-frequency (repaired in U0.4), and the design owes **visibility of changed sun state**, not friction. Read at class level only; mid-table order is decorrelation noise.
- **U.2 planned (2026-09-01)**: docs/app-ux-design-plan.md — increments with cut lines (U2.0 start screen ✅ → **U2.S shell design language** (added after the owner's verdict on U2.0's look — type scale, styled chrome via the single Chrome.button factory, spacing, the world's own palette; every later increment ships styled) → U2.1 hand-rolled three-detent sheet, retiring U0.1's scrolling bar → U2.2 the one line → U2.3 changed-sun badge → U2.4 Data tabs → U2.5 back stack → U2.6 app-strings prose gate; core = U2.0–U2.3 + U2.S). **Owner round 2 (played, 2026-09-01) overturned the sheet-as-tool-chest**: U2.R2 rebuilt the chrome as two floating overlay buttons — the intervene fab speed-dialing the four tools vertically (armed tool rides the fab in amber), and a menu fab sliding a left drawer (pace, species, utility, experiments) — while the bottom sheet became the SPECIMEN's alone, repairing a U2.1 regression the owner caught (selection had stopped showing a card). Charts got Plex Mono + spacing; dialogs got the themed surface; the layout gate learned clickable-parent/clickable-child nesting. U2.2 (one line + census dots, objective replaces it in-level, telemetry long-press moved to the line) and U2.3 (standing-sun badge in sim minutes, tap restores the founding sun, boot-gate-asserted) shipped the same day. **U2.R2b**: the specimen sheet became the mockup's rich card (structured render-thread snapshot — energy bar against the real cap, trait tiles with the trait rows' own words; boot-gate-asserted through a real selection tap). **U2.6 shipped**: harness/prose-app.js gates the app's player strings inside npm test — first run convicted 3 of 88 ('Populations'→'Head counts', 'organisms'→'creatures', and the Events title's 'because' exempted as mention-not-use, by exact name with the reason). U.2's core + U2.6 are BUILT; open: U2.4 leftovers (tab indicator is done, pager unification optional), U2.5 predictive back (waits on the D1 dependency decision). Next verdict is the owner's. **U2.0 + U2.S + U2.1 shipped 2026-09-01** (front door with the owner's stacked title; Style.kt + bundled Space Grotesk/IBM Plex Mono + restyled Chrome factory — the layout gate convicted the factory's first 44 dp build, 48 dp holds; the three-detent sheet with mode switch, pace instrument, icon lever tiles, species pills (D3 done, top strip passive), utility row, and the arm-lowers-sheet flow; bar retired from Chrome's inventory; boot gate walks detents and photographs the real screen). Pre-declared: D1 stay dependency-free (Material/AndroidX only if the sheet fights us — recorded re-entry condition), D2 line one = census+clock / objective in-level, D3 species chips go passive up top and their toggles move into the sheet at 48 dp. Zero arming-friction anywhere — the outrun study's verdict, honored. Acceptance per increment: the three screen gates + camera; phase acceptance is U.3, the owner playing the five complaints inverted into a checklist.
- **DE — the German translation (shipped 2026-09-01, owner request)**: the whole app speaks du-form German when the device locale is German, as a **display layer that never touches the core** — the core's English is certified, gated behavior (narration asserted by K6/heat gates, level text inside the hashed core), so an unmatched sentence shows in English rather than paraphrased. Three mechanisms: (1) chrome via `res/values-de/strings.xml` (every player literal extracted to resources; `Chrome.kt` keeps English inventory keys so the gates and baseline don't move, `Chrome.label()` maps key→face, `dialRowState` reads a tag instead of display text); (2) the core's words via `L10n.kt` + `res/values/narration.xml` — 19 full-match regexes over observatory.rs templates with German templates and a 29-entry trait vocabulary (locus labels, pole words, non-species impact channels; species names stay), applied at display in WorldView; (3) `assets/levels.de.json` — all six levels' player text + fail-reason map, merged in `Levels.kt` (verdicts still fall over the core's English table, identical in every language). Instruments: `harness/prose-app.js` extended (XML + overlay parsing, German rules — sentence cap + German banned list incl. rule 6's „weil"; FK stays English-only — key/array parity both directions, overlay completeness vs levels.json; **negative-tested before trusting its first PASS**), and `GermanTest.kt` on the JVM feeds every narration template a core-shaped sentence and requires German back — the alarm that rings when a core template changes and the translation falls behind (it shares BootTest's exact Robolectric sandbox signature; the JNI core fits one classloader). Record: app-ux-design-plan.md §6.
- **Screen-lock world loss (owner report 2026-09-01, root cause found + fixed)**: "when my screen locks, all data is lost" — two faults compounding, neither needing process death. A lock destroys the SurfaceView surface and an unlock creates a new one, and `WorldView.run()` founded a fresh seed-11 world on EVERY new surface (the autosave was consumed at first boot, so nothing restored); and the pause-time autosave was queued to the render thread the teardown was busy killing, so the queue died undrained and even the fallback file stayed stale — kill-and-relaunch restored (why "autosave works" tested green), lock/unlock reset. Fix: the core founds ONCE per process (`coreFounded`; later surfaces resume the living world, stale `bootWorld` never loaded over it), and `surfaceDestroyed` drains the command queue after joining the thread (the UI thread briefly owns the single-threaded core — the boot gate's own handover). New gate `theScreenLockKeepsTheWorld` plays the report back through the real view — lock, queued save, unlock paused so a kept world reads exactly t=lock and a re-founded one reads t=0 — **negative-tested: it convicts the pre-fix code with "t 0, expected t 41"**. Side effect: a lock mid-experiment no longer loses the run (the world persists in memory; levels are still never autosaved to disk, by the recorded decision).
- **U2.R3 (owner round 3, played in German, 2026-09-01)**: five device notes, all shipped — feed/kill became **armed touch tools** (tap or throttled drag feeds/erases what the finger touches; the specimen sheet keeps per-individual feed/kill, localized; no dial tool needs a selection, arming deselects), the undo chip earns a **45 s freshness window** instead of standing forever, the seed picker rows wear the **species' own colours**, the utility row became a **2×2 grid**, and the floating chrome **lifts above an open specimen sheet**. The instrument finding outranks the repairs: „Speichern" overflowed under a green gate because the layout gate measured drawer rows at device width (they ship at the drawer's 260 dp — now `Chrome.DRAWER_DP`, one declaration for app and gate) AND ran without GraphicsMode NATIVE, whose absence makes Robolectric measure text near zero wide, so label overflow was invisible by construction. Fixed, the honest gate convicted the old row 32× in BOTH languages (English was squeezed on the owner's phone all along); every profile now sweeps EN and DE. New boot-gate test `theArmedToolTouchesTheWorld`. Owner's sixth note arrived truncated ("species …") — open for round 4. Record: app-ux-design-plan.md §8.
- **EV — the evolution surfaces + the sun card (owner request, shipped 2026-09-01)**: the genetics were never unported (11 loci certified on the ported core, mutation on, the specimen card's tiles are live genotypes) — the missing pieces were the WINDOWS, now shipped: the **Traits page** (sixth Data page: per-(species,locus) mean±sd ribbon from the recorder's LOCUS_CH planes, founder line, 24-bin now-histogram in the generic tint mirrored from frame.rs, scrolling at 160dp/band), the **Evolution panel** (drawer-opened, levelAllows(4)-gated: mutation toggle, per-locus σ/curve sliders committing on release, price slopes behind a fold with the 6.1 balance marks, the four 6.3 presets as one intervention each with 'shipped' = founding σ), and the **sun card** replacing the three-button bar (light/warmth/spread sliders, the six additive layouts, +sun/+heater one-tap placement, remove-never-last). Three JNI wrappers added (evMutation/evLocus/locusGet — the C ABI had them; the bridge didn't). Boot-gate test drives all of it against the host core; layout gate measures the new grids in both locales; the prose gate convicted the browser's own Traits title ('population') — wording updated, verdict machinery untouched. Recorded gaps: evolution/sun sliders have no undo chip (the core's undo slot covers world levers only; the browser's evolution undo was UI-side), Traits patch marks, the light-budget line. Record: app-ux-design-plan.md §9.
- **SP — species Steckbrief + trait tracks (shipped 2026-09-01)**: the specimen sheet answers "what IS this thing?" — tapping the header unfolds a per-species profile (portrait from `assets/species/` bundled via build.gradle from its one committed home, README + gitignored `full/` excluded and probe-verified; role, eats/eaten-by, description), and the trait tiles grew a pole-to-pole track (`TraitMeter`: founder tick at g0, marker in species colour) plus one explanation line per locus label. All keyed by the core's English names; every slot hides when art or words are missing (species-profiles.md's contract — Mycora/Necro degrade to the dot). Strings EN+DE, prose gate PASS (330); boot gate unfolds the card against the host core and requires the portrait to decode from the bundled assets AND the unfolded block to take real laid-out space (its first photograph caught a VISIBLE-but-never-measured profile); layout gate 0 violations. Record: app-ux-design-plan.md §10.
- **DE.6 — the German writing guide + owner language session (2026-09-01)**: docs/phase8-language-style-de.md is the product, the gate its guard. Decided by A/B with the owner: world vocabulary (Planktonfresser — never bare "Fresser"; ansiedeln; Gabe; Algenmatte at first mention, then Matte; mageres Wasser; voller Teich for life-richness — "reich" stays material-only; Bestände), a personification boundary (creatures get behavior verbs, pond and water get no inner states — hungrig/Mut/im Griff/geschluckt removed; "kippt um" replaces "erstickt" as licensed Gewässersprache), the English income metaphor dies in German (Energiequelle/verbrauchen), calques out (hielt/gehalten → blieb/halte as verb), long-form imperatives, periods instead of tension dashes, three German titles reworked (Das magere Wasser, Der vollere Teich, Der schmale Grat). §8 sharpened for BOTH languages: level text has earned "weil/because" (the honesty gate IS the same-seed A/B); impact cards, narration and chrome keep the ban. `prose-app.js` now holds German to the EN word budgets, an 18-word clause cap (— and : count as boundaries), Wiener Sachtextformel ≤ 8, a German term ladder, and an umlaut-safe banned matcher — the old `\b` prefix could never match "ökosystem", and "1.300" no longer splits sentences; every new rule negative-tested before its first PASS was believed. Open: narration.xml sweep against §2b/§3, the "schaue" veto, title/science budgets (L6 EN title already breaks §4's 2–4 words).
- **Phase 8.7 — L7 The Second Sun (shipped 2026-09-01; owner request, ahead of the recorded L9-first ship order)**: dispersal limitation · colonization, built on the two remaining framework increments. **F4 timeline scripts**: `script:[{t,event}]` in the level table, fired by `levelScript()` — the per-tick hook every driver now shares (harness `drive()`, browser tick loop, app render loop): events fire before the step that produces their tick, so a scripted sun rises on its tick at any UI pace; the player's undo slot survives script fires. **F5 region goals**: `{m:"near",sp,src,r}` predicates/meters — census within toroidal radius r of source src, captured by levelScript one tick before each recorder sample into a level-owned ring; `levelCheck` consumes samples only up to the census watermark, so verdicts stay caller-cadence-independent; squared-distance arithmetic only, bit-identical on the ported core. **Per-source lock**: apparatus `sources:"added"` + `levelAllowsSource(k)` — the founded sky locked, the risen sun editable — wired into every grip/drag/slider/layout/remove path in browser AND app (published as `homeSunLocked` for the UI thread). The level: settled core (S120 D500 C12 B60, seed 101, pinned for slowest creep), second sun at (0,0) at t=2,000; goal Solara ≥ 100 ∧ Drifta ≥ 150 within 200 of sun 2 by 12,000. **Measured**: plankton never crosses dark water (0 through 16k); the mat's creep arrives only from ~t=12k (so 7.L's "inert until seeded" is true on its window; deadline sits before the creep); pours are not propagules (1/0 at 12k); **Drifta-only seeding locks the mat out entirely** (0 through 16k — L4's competition lesson as the second wrong lever); late expedition t=9,000 still passes. Gate 30/30 ALL PASS incl. 4 apparatus-lock guards, **byte-identical on the ported core**; port:check's level fingerprint extended over the sunrise (2,200 ticks); conform + conform:core rebound (declared; fingerprints bit-identical ×4, native == wasm); boot-gate test drives the sunrise through the real render loop and the refused home-sun grip through the real gesture pipeline; DE overlay complete, prose gates EN+DE green; headless Playwright playthrough of both paths (null → honest fail; pan + seed both → pass) — **which convicted a latent browser bug on its first run**: `reset()` set `ui.chips` to the truthy `[]`, so every in-level Try again and sandbox reset crashed the React tree; fixed (`chips: null`). Record: phase8-levels-plan.md §3. Known edge recorded there: the goal region tracks source index 1.
- **Phase 8.9–8.12 — the ladder completed: L8, L9, L11, L12 shipped; L10 folded (2026-09-01)**: the whole batch calibrated ON THE LEVEL MACHINERY after the gate convicted raw-stream pins twice — that is now the standing calibration rule for levels. **L9 The Sorting** (first evolving-world level; the pack-pressure design died twice — crash-grazing sweeps FAST or nowhere, the null self-sorts by ~22k — rebuilt around the armor-price lever: kp under its balance mark sweeps tough 10.3–12.8k vs deadline 18k; wrong lever = mutation OFF, flat 18k; two overturned pins recorded as findings: extra grazing is stream-chaotic and hand-feeding the tough IS artificial selection, 0.92/100%). **L8 The Warm Year** (ramped +5 sun bills the APEX first; the spec's cold-refuge rescue is measured dead — every grazer death traces to the FED apex, not the heat; shipped as the husbandry-dosage lesson: feed lightly 900/0.2 → both hold, PASS @10.8k; feed hard 300/0.3 → the pack eats the middle; cold-alone and pours fail). **L11 The Refuge** (three scripted mesh-pen sides at t=600, the player clears then closes the fourth — clean-close is airtight 3/3 seeds, penned C exactly 0; closed dirty it is a PERMANENT self-sustaining feedlot 4/4, a lobster trap; the spurious refuge-emptied latch was removed after it convicted itself — sustain-150 carries robustness; the only fully seed-robust level of the batch). **L12 Outrun the Sun** (the capstone re-pinned one trophic level up: at +8 CILIO dies in ~800 ticks and nothing rescues — MV.1's numbers are stale; at +6 the null loses the core 4/4 and CILIO's warmth-preference σ 0.12 raised before the press rescues 3/4; pinned seed 11: σ early passes at ALL pre-press timings, σ after the heatTrap fires fails @7.1k). **L10 folded** per the ladder's own cut line: asymmetric grazing no longer separates patches on the movement-genome core (MV.2 restlessness — gene flow beats patch selection); finding + re-entry condition in the ladder doc. Machinery added for the batch (both cores): `world.mutation`, metrics `ch`/`share`/`at`, script events `sourceSet`/`wallAdd`/`spawnPack`. All records: phase8-levels-plan.md §3.
- **Owner round 4 (2026-09-02, both reproduced before repair)**: the **world-boundary seam** — per-tile drawBitmap clamped bilinear filtering at tile edges (and the 4x field prescale clamped the same way), a 2.4x pixel-difference spike where the wrap crossed the screen; fixed with REPEAT BitmapShaders at both levels (painting only, frame.rs untouched), gated by `WorldSeamTest` (negative-tested: convicts old at 2.42x, passes fix at 1.03x; the browser keeps the same clamp seam as the renderer's frozen oracle, recorded not patched). And **experiments now save**: snapshot format v2 carries the level runtime (keyed by level NAME; census ring stored, rg_def derived via the factored `collect_regions`; v1 files still load, as sandbox), proved in the extended `port:snapshot` (L7 saved mid-run resumes bit-identically across its deadline verdict; re-save byte-identical); the U0.6 "levels are never autosaved" decision is REVERSED (it existed only because the snapshot lied) — experiments autosave to their own `experiment.mcsm` (the kept sandbox pond can never be clobbered), boot prefers it, the shell adopts whatever a load carries (`adoptCoreLevel`, also fixing lost experiment context on activity recreation), and the front door grew a "continue the experiment" row. Core baseline rebound (declared, fingerprints bit-identical ×4). Record: app-ux-design-plan.md §11.
- **TH — experiment-menu thumbnails (owner request, shipped 2026-09-02)**: one gameplay-captured picture per level on BOTH menus. `tools/level-thumbs.js` (`npm run thumbs`) plays each level through the real browser UI (playthrough-style: dev server + headless Chromium + genuine pan/zoom gestures) and photographs a curated moment per a per-level shot spec — tick, world point, zoom, and scripted acts where the actor is absent from the null run (L5 seeds Cilio, L6 the Venator pack with hatch time). `actor:` specs are exact: the level run is deterministic, so the tool replays it headlessly to the paused tick (levelStart+levelScript+step) and centres on the species' densest cluster. One committed home `assets/levels/<key>.jpg` feeds the APK (Profiles.levelThumb + PortraitView row) and the artifact (generated `src/ui-thumbs.js` data URIs, +67 KB). Missing file = no picture, never a placeholder. Conformance bit-identical; boot gate requires a laid-out thumbnail in the experiments panel. Not bit-reproducible between runs (live render loop) — a curation tool, not a gate. Record: app-ux-design-plan.md §12.
- **GR — organism graphics (research → plan → GR.1–GR.3 shipped, 2026-09-02)**: docs/organism-graphics-research.md (four techniques weighed against the frame-grammar contract; owner verdicts: textures yes, zoom-LOD yes, low poly and clock animation no; darkfield stays) + docs/organism-graphics-plan.md + the probe `dev/graphics-probe/stilproben.html` (four rounds, the design record). All painting-tier, app-only, fingerprint-frame untouched by construction. **GR.1** Venator = *Didinium nasutum* (researched §10 — the real twin of the Venator–Cilio pair; barrel, rod-palisade proboscis, two pectinelle girdles, band macronucleus; Ghost Ray retired from the app, browser oracle keeps it) — owner-accepted on device. **GR.2** micrograph sprite bakes (membrane/nucleus/vacuoles; defense = spines, feeding reshapes at constant area) + `SpriteSheetTest`, which convicted twice before its first green (invisible Bacillus dial; rods merged in its first photograph) — owner-accepted on device. **GR.3** zoom ladder (crisp vector overlay over the blit above 28 CSS px; carpet cells above zoom 2, coloured from the core's own field pixels so ramp + light-locus turn survive by construction; hash layout, zero PRNG) + `WorldCameraTest` photographing the real world at three zooms in CI. **GR.4 closed the phase (2026-09-02)**: all three increments owner-accepted on the device (GR.1 "sieht super aus", GR.2+GR.3 "very good"); the owner's Didinium portrait shipped as `assets/species/venator.jpg` (640², original in gitignored `full/`), boot-gate-decoded. Watch items, not blockers: device budget numbers never read out (no jank reported), far-zoom mat blockiness (UP=4 diagnostic) awaits an explicit verdict. Per-individual variation stays a declared grammar change (id field), recorded in plan §3. **Post-closure rounds (same day)**: perf triage instrument-first — the owner's 32 ms turned out vsync-quantized (the HUD timer measured `unlockCanvasAndPost`, A.1's own recorded trap; fixed: the line reads true `work` ms + a cells count; his re-measurement: 1.7–3.9 ms everywhere, budget met); Bacillus rebaked as an axis-chain rotated by the record's heading (the fixed constellation read as "Formationsflug"); and **GR.5, the phase's one declared frame change**: between-tick display interpolation became a quadratic B-spline through the last two segment midpoints (velocity-continuous — the 1× plankton "springen" was the walk's own 10 Hz kinks, not dropped frames), `ppx/ppy` render scratch shifted by mark_prev on both cores, curve arithmetic inside the frame fingerprint, conform:core rebound declared, port:check bit-identical — owner-accepted ("klappt gut, flüssig"). **GR.6** (second declared frame change): the corpse record grew to 6 doubles (`sp`, `fresh` = mass/size — the sim's own decay clock); the app paints a collapsed membrane ghost that wears its species colour fresh, greys and deflates as it rots — death reads as a collapse, not a pop; eaten prey still vanishes at the bite (swallowed). Same proof chain, all bit-identical.
- **GR.7 — the light field (owner request, shipped 2026-09-02)**: a second microscope for the same world, switchable from the front door and the drawer. Microcosm has always been a darkfield micrograph; brightfield is the other classical regime — the ground is the lamp and a body is what absorbs it. The look was NOT new work: the probe's Hellfeld tab has existed for exactly this comparison since round 2, and its recipe went in verbatim (`#ECE9DF` ground, half-fade to a stained slide's mid grey, `x0.55` for structures, one charcoal ink, the white phase-contrast rim in place of the bloom). What was missing was a renderer able to hold two regimes: the probe is a standalone page with its own mini-simulation and shares no code with the app, by its own design. `Optics.kt` is the seam — three rules the painters route their colours through (**wash** a fill, **line** a structure, **ink** the contrast), all three the identity in the dark field. That identity is measured, not assumed: the world camera's three rungs are byte-identical to main's, 0 of 1,713,600 px differing at every rung. Five structural differences only, each commented where it is made (bloom→rim; blits drop SCREEN, which on a lit ground washes every body white; the light layer paints a lamp; the wall's backing follows its ground; Bacillus's capsule glow becomes a stain). A rebake per regime rather than a MULTIPLY composite — the bakes are premultiplied ARGB, and MULTIPLY drives their transparent surround to black. **The core is untouched**: same display list, no fingerprint moved, browser oracle unchanged — a second ground is painting, which is where a look belongs. The optic is the app's only preference, since it belongs to the player's eyes and not to any pond. Gates extended rather than added: the sprite sheet bakes, photographs and holds its dials in BOTH regimes and asks the question a second ground raises (can this body be seen on the ground it lands on); the world camera shoots all three rungs twice; the boot gate taps the switch. One recorded concession: amber stays the hand (rule 7) and takes a deeper tone on the lit ground, because `#F2B24A` there is a pale mark on a pale ground. Two watch items for the device, not tuned blind: Cilio's membrane reads heavier in the light field (bake stroke and vector overlay coincide by design, and SCREEN used to soften the stack), and the lit pool reads slightly cool under the mineral field's blue. **Found while proving the dark field unchanged, pre-existing and not fixed**: `WorldCameraTest`'s frames differ between an isolated and a full-suite run — 98% of pixels, on main, with no code change involved; something earlier in the suite survives `boot(); resetWorld(); initWorld(11)`. Harmless while the camera only asserts that something was painted; disqualifying if anyone ever wants it pixel-stable.
- **The help page (owner request, shipped 2026-09-02)**: an overview for a beginner and the depth a curious player asks for afterwards, reached from the front door. Three things make it a page rather than a wall of text. A **drawn diagram** of the mineral's round — four stations on a ring, with the one leg a living thing has to walk (remains back to water) highlighted in Bacillus's own colour and carrying the only label, since that leg is the whole K6 lesson. The **portraits the repo already owned** (assets/species), one per card. And a **card per creature** that puts what it does here beside what its real model does out there. `Help.kt`, ~350 lines; every colour from the core's bucket table, every word a localized resource, every picture the committed art — a species renamed in the core surfaces as a missing profile, never a wrong one. **The real models, researched**: Solara ← the algal mats a lit floor wears (*Cladophora*), Drifta ← drifting single-celled green algae (*Chlamydomonas reinhardtii*, eyespot and phototaxis), Cilio ← *Paramecium* (oral groove, food vacuoles, the avoiding reaction — which IS Cilio's flight from alarm scent), Bacillus ← *Bacillus subtilis* (run-and-tumble, hydrolytic enzymes, the endospore that waits), Venator ← *Didinium nasutum* (already the owner's decision; pexicysts then toxicysts, swallowed whole). Each fact was chosen because it maps onto a mechanic the sim already has, which is what makes the page teach rather than decorate. Sources named per card and listed again at the end, with the licence and the retrieval date. **Caveat recorded, not smoothed**: Wikipedia is unreachable from the dev container (egress-blocked), so the facts came through search summaries OF those articles rather than from the articles opened directly — worth one spot-check before release. **The language rule this changed**: the prose gate's banned list exists to stop chrome lecturing a player who did not ask; the help page is where a player DID ask. So `help_*` keys, and only those, run in a new **reference register** — vocabulary free, clause caps unchanged, readability held to grade 12 rather than waived (docs/phase8-language-style.md §11, -de §9). The gate refused the first draft eleven times on sentence length, and every refusal was a real fix. **Gates**: `HelpPageTest` photographs the WHOLE page at 408 dp in BOTH languages (4,693 and 4,878 px tall — German runs a fifth longer, which is exactly why it is shot separately) and asserts five portraits, five sourced model notes and the licence line; the boot gate opens the page from the front door, checks all five species are profiled and that the diagram is drawn rather than described. **Found while building it, still open**: `MainActivity.speciesColor` — the drawer's identity dots and the seed picker — reads bucket (0,0), which is a RAIL of the tint dial rather than the species colour; it paints Bacillus's olive (158,168,104) as rust (137,85,63). The help page uses the middle bucket (the founding genotype) and is correct; the drawer was left alone as outside this task.
- **The standing-sun badge leaves by itself (owner report, fixed 2026-09-02)**: "move the sun and the undo bar stays up forever". U2.3 shipped the badge deliberately persistent — and a moved sun never un-moves itself, so it was permanent by construction: the round-3 undo-chip finding (an offer, not a monument) one round later. The badge now wears a freshness window — any change to the sun re-arms it for 90 s of real time (twice the chip's 45 s, so the notice still outlives the offer to put the world back), nothing else does, and a re-baselined sky (founding, level start, load) wears none. Recorded costs: the founding-sun restore hangs off the badge's tap and goes with it, and the outrun study's conviction is answered only while the notice is up. Boot gate extended to the full life (appears → clears on restore → leaves on its own while the change stands → re-arms on the next touch), window shortened through `world.sunBadgeShowNs`. App suite green, npm test green, conformance bit-identical. Record: app-ux-design-plan.md §13.
- **The help page, second pass on the facts (owner review, 2026-09-02)**: the owner brought an outside editorial review of the page (Wikipedia-checked, German). Acted on where it convicted a real claim, argued with where it was reviewing from thinner sources than the repo already held. **Fixed, because the claim was not supported**: Solara's "real mats grow so dense that light, oxygen and mineral fall away within a few centimetres" — *Cladophora* is now described by what the cited article carries (branching filamentous green alga, worldwide, fresh/brackish/salt, heavy mats where dissolved mineral is plentiful), and the self-shading it justified is now stated as the POND's rule, which is where it always belonged. Cilio's "Cilio's flight from alarm scent is that reaction" equated a game mechanic with *Paramecium*'s avoiding reaction; the avoiding reaction stays (it is in the cited article), the equation goes, and the card now names both simplifications outright — that Cilio lives on Drifta where the real one lives mostly on bacteria, and that the alarm-scent flight is a rule of this game. Venator's "toxicysts kill it" softened to "subdue it" (and DE spelling Toxicysten → Toxizysten). **Kept against the review**: the pexicyst/toxicyst mechanism itself, because the review checked Wikipedia and the card cites Wessenberg & Antipa 1970 — a primary source the review did not have; likewise Didinium's 80–200 µm, which comes from that paper rather than the 50–150 µm the German Wikipedia gives. **Declined, and why**: the review's central editorial proposal is a vocabulary swap — *Nährstoff* for Mineral, *Baustoff* for Protein. Both are more transparent words read in isolation, and both are refused here: `phase8-language-style-de.md` §3 decided one-thing-one-name and bans "Nährstoff" for exactly this slot, the app's levers, Data pages, narration, impact cards and twelve levels all say Mineral, and a help page that alone speaks a second dialect teaches the player a word the world will never repeat. The reference register would have let it through the gate, which is precisely why it is a judgement and not a gate question. Owner's call to reopen; it is a global rename, not a help-page edit. **Added**: the house rule now states two rules rather than one — energy comes in with the light, passes on with the food and leaves as warmth; mineral only changes address and never leaves the pond (the review's best point: the old "nothing is made, nothing is lost" was true of matter and quietly false of energy). A **size** chapter, because size was named in "four accounts" and then never explained, and it scales real machinery (energy cap, mineral quota and uptake, photosynthesis, move cost, feed rate, the protein threshold for dividing — `step.rs` `w.sz[i]` at twenty draw sites). The food-web chapter now says the web is simplified on purpose, and the species lede states the "In the pond / In reality" split as a rule rather than leaving it to the labels. Drifta's card gained the nitrogen-starvation → resting-zygote leg, which is what the pond's cyst is actually built on. DE also took the style guide's own decided word: "der Teich kippt langsam um", not "erstickt" (§2b). **Seam recorded**: `help_titles` grew from 12 to 13, so `Help.kt`'s chapter slices are no longer bare 3/9 literals but `AFTER_SPECIES = 10` with a comment saying what it marks — the old code would have silently pushed the new chapter below the species cards. **Gates**: `npm test` green, conformance bit-identical (nothing the tick reaches was touched), app prose gate 503 strings PASS in both languages. **Not run, and not claimed**: `HelpPageTest` — Maven Central answered 429 to the container all through the session, so the Android app could not be compiled here; the change is text plus one named constant, and the test's assertions (five portraits, five sourced notes, the licence line) are untouched, but the page has not been photographed at 408 dp since the edit. Worth one CI run before release.
- **The sun leaves the sun (owner report, shipped 2026-09-03)**: "placing new suns and sun scenarios are now hidden behind the sun settings — I need to click a sun first to place a new one." EV shipped the sun card as the SELECTED source's card and nothing else opened it, so the two levers that make a sky could only be reached through a source that already existed: the lever's entry point was the thing it creates. The sun is now the intervene dial's fifth tile (appended, so the four older indices keep their meaning — the boot gate keys on them) and opens the sheet **cold**: title *sky*, the six scenarios and the two placements, the three per-source sliders hidden because nothing is gripped; gripping a source deepens the same sheet into the card it always was. Dimmed and refused where a level keeps the sky (`levelAllows(2)`, published as `world.sourcesAllowed` — the first dial tool that can be taken away); L7's founded-sky lock unchanged. **Owner's second round the same day, on the first build**: the dismiss is now the specimen sheet's small cross, unboxed and dim, in the same corner and from the same `Chrome` factory (one dismiss glyph, not one per sheet), with `remove` beside it behind the specimen row's 14 dp gap; the placements became two icon tiles sharing the width, because three weighted buttons wrapped "+ heater" in both languages; and the six scenarios stopped being six identical text buttons — each is a card carrying **the sky it would make** (`Sky.kt`'s `SkyPreview`, painted from the same five numbers per source the tap applies, so the picture cannot drift from the act). The first preview was true to the torus and useless as a picture — a source at (0,0) genuinely belongs in all four corners and drew as a wash — so the cut moved instead: sources are taken at the copy nearest the first and the view is rolled until that constellation is centred, which makes two suns read as two and the archipelago as three. Ember and cold are `Layers.kt`'s own; light is sunlight yellow by the owner's call, the one departure from the darkfield's pale blue, kept clear of the hand's amber and confined to this picker. Gates: layout gate now measures the sheet's rows at the sheet's inner width (`Chrome.IN_SHEET`) — 0 violations on four devices in both languages, and it convicted the card's first draft at 320 dp in German; boot gate walks dial → cold sheet → armed placement → cross. `npm test` green, conformance bit-identical (the core is untouched). Record: app-ux-design-plan.md §16.

- **The drawer, re-laid (owner request, shipped 2026-09-03)**: four asks, all of them about where a control sits rather than what it does. **The optic is a switch now**, on the front door and in the drawer — GR.7 shipped it as a card and a full-width button, which is the grammar of a door, and a door that comes back to the same screen with a different ground was never one. `Chrome.switchTrack`/`switchRow` generalize the canvas's mode switch, and the amber/slate choice is remembered on the track rather than passed around: amber marks the player's hand and nothing else (rule 7), so a switch that only changes what the world LOOKS like gets the slate one, and its word stays lit in both states because both are a microscope you are looking through. **Home, save and reset are glyphs at the drawer's head**, pinned above the scroll — they were four words in a 2x2 grid at the foot, which spent a third of the drawer's height on its two most-used controls and offered no way home at all. Home takes the same path as back-at-top-level (autosave, then the front door), so leaving through the drawer costs nothing. The wordmark went with them. The loss is paid for, not waved away: each glyph carries the word it replaced as contentDescription and long-press tooltip, and reset's two-step guard — which used to say "sure?" in the button — now goes amber AND says it in a toast, since a guard whose only first step is a colour change is a guard the player has to already know about. **Data and Evolution are tiles at the foot**, icon over word, pinned under the scroll. Stacked rather than side by side for arithmetic and not taste: each tile gets ~126 dp inside the drawer, and an icon beside "Evolution" at 14 sp does not fit there in either language — the same sum that overflowed "Speichern" once already. **Experiments left the drawer**: the ladder is the front door's second row, and a menu entry that duplicates the door is a second door. The view switch moved above the species list in the same round, because the first drawer photograph showed it below the fold. Gates: `Chrome.ROWS` grew `drawerTop`, `drawerNav`, `optic` and `bench` and lost `utility`, so the layout gate and the row camera measure exactly what ships (0 violations, four devices, both languages); the boot gate asserts both new rows and that home really goes home; 18 app tests green, conformance untouched (no core file moved). **The front door scrolls now** (owner, same day, after the finding was reported): it had been growing rows — the continue row, help, the optic — under a wordmark that takes 145 dp on its own, and at 320 dp wide the last of them were laid out past the bottom edge with nothing on screen to say they existed. `isFillViewport` keeps the old look wherever the door still fits: the panel is stretched to the viewport when the content is shorter, so its CENTER_VERTICAL still centres, and only a door too tall for the screen scrolls at all. The panel is a `StartDoor` — a LinearLayout that carries its scroller's visibility — rather than a wrapper toggled at each of the shell's (and the boot gate's) `startPanel.visibility` sites: the first line that hid the panel and forgot the scroller would leave a full-screen invisible view over the pond eating every touch, and that is a rule nobody can enforce by hand. Reopening the door returns it to the top. The boot gate now asserts that the first row starts inside the door and the last ends inside it, which is exactly what was false before; its front-door photograph grew from a clipped 320x470 to the whole 320x652 door.

- **The optic's preview (owner request, shipped 2026-09-03)**: "a representative icon / simplified preview instead of an explanation, switching between the two". The front door's optic row carried a sentence under the switch ("glowing life on black water — tap for the light field"); it now carries a 52 dp field of view. Two findings, one failure: the row was saying three things at once. A switch's position already states the side you are on, so a line beside it naming the ACTION is the toggle's classic ambiguity — the reader cannot tell whether the control reports a state or offers a command, and the standing advice is to label the thing and stop writing the rest (UX Movement, *The Confusing State of Toggle Switches*; Cieden's switch guidance). And where the outcome is a look, show the look — the Preview pattern, whose everyday case is the appearance pickers in macOS and iOS. The row is now picture, state, switch, all three saying the same thing. The label lost its "view:" prefix with the sentence (`dark field` / `Dunkelfeld`), which also stopped the drawer row from repeating the section header above it. **What makes it honest rather than decorative**: not one pixel is chosen. The ground is `Optics.ground`, every body goes through the same three rules the renderer paints the world through, and the one structural difference between the regimes is made the way `Renderer` and `Sprites` make it — the dark field's additive bloom becomes the phase-contrast rim. Two hand-drawn icons would have been a second definition of the look. **The trap it walked into first**: `Optics.lightField` belongs to the RENDER thread — the menu writes `world.lightField` and the render loop applies it a frame later — so a preview reading the global paints the microscope the player just left, and flipping the global from the UI thread would both race the renderer and cancel the real switch, which is guarded by comparing against that flag. So each rule grew a form that takes the regime as its first argument, the flag-reading forms delegate to it (one recipe per rule, unchanged behaviour), and the picture is right in the same frame as the tap. The boot gate measures exactly that: the swatch's mean luminance in both regimes, which convicts a preview that paints nothing, paints the same picture twice, or lags the tap by a frame; plus the thumb's side, asserted rather than looked at. **Not done, deliberately**: the drawer keeps switch and word with no preview. It would be a fourth signal in a row that already has three, which is the mistake this round removed from the front door. **One flag for the owner's eye**: with the prefix gone, "dark field" sits in a list of destinations (sandbox, experiments, help) and could read as one; the circle and the switch are what say otherwise.
- **Next (owner order)**: the ladder's shipped set is L1–L9, L11, L12 (L10 folded — its content awaits an L9 twist run once F3 builds). Playthrough paths exist for L7 and L9; L8/L11/L12 paths queued (wall drawing, feed cadence and σ sliders need gesture scripting). Deferred with re-entry conditions: F3 post-pass twists, F6 A/B memory, L10's geometry (a wall between the shores). All player text obeys phase8-language-style.md and, in German, phase8-language-style-de.md (both enforced in npm test). Also queued: post-phase Yoshida study, third loci where a question warrants them, a split-detector once a split is observed in a shipped world; Mycora/Necro re-entry conditions unchanged.
- **Phase 9 S — the sandbox start worlds (owner request, shipped 2026-09-03)**: the sandbox had one opening (the shipped pond on a random seed) and therefore taught one lesson. It now has **five**, chosen at the front door: `pond` (the certified world, composing nothing at all), `still` (one sun, full mineral, nobody home), `twosuns` (suns at x=256 and x=768, i 1.1, σ 140 — the SAME income as one sun, only the geography differs), `refuge` (the fine-mesh pen `harness/walls.js --hideout` already measured) and `shallows` (M0 1.7 instead of 2.2). **Where they live**: `rust/microcosm-core/src/starts.rs`, as data — one definition, reaching the app as JSON exactly as the level table does (player text is keyed by the core's key in strings.xml, EN+DE, through the prose gate) and reaching every harness as the same JSON, so what was calibrated IS what ships. **What a start may compose**: the `init_world` scenario and ordinary events, nothing else; three things it may never do (touch `P.mutation`, leave the undo slot loaded, log an intervention) are gates. **The identity gates license the rest**: `pond` founded through the new path is the shipped world bit for bit on seeds 11 and 88, and every start the crate founds equals the same start composed out of ordinary events — which is what makes `--sweep` (a JavaScript composition) a measurement of the real thing. **Calibrated** over 8 seeds × 18,000 ticks, tune2's criterion unchanged: all five hold the four core species 8/8; apex held 6/8 pond, 4/8 twosuns, 2/8 refuge, **0/8 shallows** (lean water establishes a hunter and loses it again on every seed — reported, not gated). `still` has its own criterion: stays empty 8/8, audit exactly flat, the Observatory answers in nulls not NaN, and a scripted player settles it 8/8 (the grazer took on 5/8 — a four-organism pack is a coin flip, which is E6's lesson, so it is reported rather than required). Sweeps: 135 two-sun runs (σ 210 cannot make two pools on a 1024 torus at any separation; 512 apart is the symmetric case, peaks 1.128, both straits 0.444), 27 pen runs, 18 mineral runs (the lean cliff is sharp — 1.4 collapses 1/3, 0.8 collapses 3/3). **The start that did not ship**: a hot spring. 36 runs, 12 collapsed, and the second sweep showed why it is not a matter of "too hot" — a=0 is the pond exactly, and a vent of ANY amplitude, warm or cold, mean warmth as low as 0.06, flips the world into a plankton-dominated regime with a fragile decomposer. The shipped pond is isothermal by construction, so its temperature gradients are exactly zero and thermotaxis is inert; the first vent turns directional movement on for Drifta and Cilio at once. Consistent with where heat already lives (E8, E12 use warmth as a press to survive); deferred with a re-entry condition (compose founding counts that pay for the flip, hold 8/8). **App**: the sandbox row opens a water chooser (kept pond first when there is one), the chosen start is remembered across launches, and the reset button re-founds THAT water rather than the shipped pond. **Gates**: `npm run starts:check` in `test:port` and CI on every push, the acceptance run beside `tune` in the manual ecology job. npm test green, conformance bit-identical on both cores, port:check still identical, core baseline rebound (declared: `starts.rs` added, fingerprints unchanged). Record: docs/phase9-starts.md.
- **Phase 9 S — the start worlds get their pictures (owner request, 2026-09-03)**: each chooser row now carries a photograph of the world it opens, the way each experiment row does. The tool is `StartThumbsTest` (`gradle -p android-app testReleaseUnitTest --tests '*StartThumbsTest*' -Pthumbs`), a curation tool in the app's test source set — the only place the real renderer runs without a phone. It founds each start on seed 11, steps it headlessly to a fixed tick and paints one frame through `Renderer.draw`, the painter that will show the result. Two deliberate differences from `tools/level-thumbs.js`: the app's own painter rather than the browser's, and **reproducible** (fixed seed, fixed tick count, no live loop — the same jpg every run). Shot list and what each picture means: docs/phase9-starts.md §7. `twosuns` is the one shot no phone could frame (the app clamps zoom-out near 460 units, its suns stand 512 apart) — said where the number is set rather than implied. Gates: the tool asserts each picture painted something over the abyss; `StartsTest` asserts every start still loads its picture. **Environment note that overturns the previous entry's caveat**: this dev container DOES have an Android SDK (/opt/android-sdk) and gradle, so the whole app suite runs locally — the one obstacle is Maven Central answering 429 to the shared egress IP, which a local-only gradle init script fixes by serving Central from its Google Cloud Storage mirror (`maven-central.storage-download.googleapis.com`, same artifacts, same coordinates). 20 tests green locally, including the boot gate. **The environment gap that caused it, measured and closed**: a cold session downloads ~835 MB of Gradle cache and ~350 MB of Robolectric android-all jars, and its first three gradle runs cost 1m37s (failed on Central's 429), 2m16s (three tests failed fetching the jar) and 38s (clean) — bandwidth was never the problem (34 MB/s through the proxy), cold caches and Central's burst throttle on the shared egress IP were. `tools/setup-android-env.sh` now mirrors BOTH Maven clients (Robolectric fetches its jar with its own client, which never sees Gradle's repositories — the two properties are `robolectric.dependency.repo.url` and `.repo.id`, read out of the shipped jar and then verified by deleting the cached jar and watching it return from the mirror), adds the wasm target, and warms every cache from the checkout so the snapshot carries them. Numbers and the two dialog changes: docs/android-dev-environment.md §8.
- **(was) Next: Solara light-adaptation locus** (shade-tolerant ↔ sun-loving; factor `1 + s·(g−g0)·(1−2L)` inside the photosynthesis block, priced by the light field — the sun lever becomes a selection pressure), then **Bacillus rate–yield** (voracious ↔ frugal; `rateE` up, `effE` down). Each its own declared change: expression at existing draw sites only; `silent` fingerprint identical; price by measured surface before shipping; re-accept with tune2 + corridor (16 corners) + gate5. Then multi-locus (`W.g[k*MAXN+i]`, per-locus channels, shape/outline encoding). Venator stays excluded (decision 3).

## Brief history (details in docs/)

P1 built the living pond and the observation UI. P2 built conserved chemistry, corpses and detritus, and Bacillus — its crown finding was the K6 experiment (decomposers off → slow mineral strangulation), which became the ground truth for everything after; also the transport discovery (mixing feeds the core) and the M-audit discipline. P3 added Cilio flee/alarm and Venator (pack founding, after five failed establishment designs — an apex predator is knife-edged by nature; accepted with a natural-turnover criterion), deferred Necro and Mycora with named re-entry conditions, and dropped barriers when the project refocused on monitoring. P4 built the Observatory: ring-buffer recorder (pure observer, bit-identity enforced), detectors, calibrated indicators (every detector's first theory died against measurement — absolute thresholds, EWS statistics, and level-based depletion all failed; shipped: mechanistic vitals against measured reference bands, plus trend+level+destination depletion logic), levers with honest provenance and undo, five chart pages, impact cards (ITS baselines, natural-variability tiers, press-backdrop flags), and passed the narrate-the-collapse gate. Post-P4: research and plan for heredity; a tweak batch (reset, seeding, Ghost-Ray Venator — final form a single heading-aligned spearhead, wake animation cut as beside the point); and the integrity incident with its hash-binding fix.

The project's meta-lesson, worth carrying into every future phase: the instrument is only as honest as the calibration fights behind it.

- **The undo chip stands 10 s, not 45 (owner instruction, 2026-09-03)**: "all undo buttons after
  placing, moving suns, editing etc. should show no longer than 10 seconds." The window from
  U2.R3 is cut to `WorldView.UNDO_SHOW_NS` = 10 s — one named constant beside
  `SUN_BADGE_SHOW_NS` instead of the literal that sat in the frame loop, plus `world.undoShowNs`
  for the gate to shorten. One chip covers every lever (pour, seed, feed/kill, sun, walls): the
  freshness clock hangs off the core's `ivCount`, so this is a single number, not a sweep. The
  browser build's chip was already 5 s and is untouched. The standing-sun badge stayed at 90 s
  in this change — cut to 30 s in the next one, same day —
  it is a notice that a change stands, not an offer to put it back, and §13's point was that it
  outlives the offer. Recorded cost, not smoothed over: an intervention whose consequence needs
  half a minute of sim time to show is now un-undoable by the time the player sees it; if that
  bites, the answer is a permanent per-lever restore row, not a longer window. Boot gate
  `theArmedToolTouchesTheWorld` extended — a fresh intervention arms the chip and it retires on
  its own while the change still stands. Core untouched, conformance bit-identical. Record:
  app-ux-design-plan.md §14.

- **The standing-sun badge goes after 30 s (owner instruction, 2026-09-03)**: straight after the
  undo-chip cut, and against what that entry had argued for leaving alone. `SUN_BADGE_SHOW_NS`
  90 s → 30 s. The argument survives the instruction: the badge is a notice that a change is
  standing, not a button that puts anything back, and at 30 s against the chip's 10 s it still
  outlives the offer three times over — U2.3's ordering holds, only the distance changed. What
  goes is the long tail, the minute and a half in which the bar reported a change nobody was
  still deciding about. Sharper cost, restated rather than buried: `putSunBack`, the one path
  back to the *founding* sun, hangs off the badge's tap, so it is reachable for 30 s now instead
  of 90; past that nothing on screen offers to put the sky back where it started. The answer, if
  it bites, is the permanent restore row §13 already named, not a longer bar. Gate unchanged and
  still honest — it drives the badge's whole life through `world.sunBadgeShowNs`, proving the
  mechanism rather than pinning the number. Core untouched, conformance bit-identical. Record:
  app-ux-design-plan.md §15.
- **U3 — the Data screen redesigned (shipped 2026-09-03, owner review)**: five screenshots and five sentences — the charts are too huge with no free space, the labels are too big, there is no legend, the text pages are almost raw, the row of pages on top steals the screen. All five were true, and all five had one cause: the browser's chart code (`src/ui-data.jsx`) was ported to Kotlin as *drawing* and never as *design*. What shipped, worked through the data-viz method (form → colour → validate → marks → labels, and only then look at it):
  **The frame.** The two-line page title and the six 48 dp hairline buttons became a header (`Daten` + ⓘ + ✕) over a real tab strip — text tabs, a 2.5 dp underline on the selected one, `Chrome.tabs`/`tabSelect` scrolling it into view, a fading edge saying the row scrolls, the swipe still driving the same state. The bottom's full-width "close" row is gone; the ✕ and the back gesture are the two ways out. The page's explanation moved out of the permanent header into the ⓘ card (still `page_titles`, so the translations and the prose gate's TEACHES entries are untouched).
  **The plot stops eating the view.** It was `height - padT - padB`, i.e. whatever container it was handed. It is measured now: bounded by its own plotting width (0.55–1.0 of it), then the axis band, then the legend, then air. Type is in sp through `Style.sp` — the axis labels had been `22f * density`, twice a caption and deaf to the player's font-size setting.
  **Every colour gets a name.** A legend row per series: dot, name in ordinary ink, current value in mono, and on Chemistry the share. Not decoration — the species palette is the WORLD's (rule 7, and a chart that recoloured the creatures would break the only link worth having), and measured against the data-viz checks it collapses under deuteranopia (Cilio↔Drifta ΔE 1.9) and is thin even in full colour (Drifta↔Venator ΔE 8.9, floor 15). Since the hues cannot move, identity arrives through a second channel: the legend, the value column as a table view, and a direct end-label per population line. Chemistry's four compartments measure ΔE 9.2 deutan / 15.3 normal — above both gates; they fail only the lightness-band and chroma-floor checks, which is deliberate (corpses and detritus are meant to be dull). Metabolism's pair measures 13.6 / 21.1 — and a saturated green/red "improvement" collapsed to 5.2, the classic red-green confusion, which is why the burn line stays a muted tan.
  **Metabolism stopped lying.** It drew production and burn on one scale and recycling on a second, invisible one in the same box — a dual-axis chart, the single most-flagged mistake in charting. Two stacked panels sharing one time axis.
  **The two written pages are pages now.** `DataPages.kt`: Vitals is a built tree — five stat tiles, then a row per species with a meter, the reserve and its trend, the population factor, and the state as a WORD beside the colour (calm/tense/critical; the ladder skips amber by rule 7, and the meter carries the species colour so brightness never reads as "healthy"). Events is one styled `Spannable` — the clock a real mono column, the sentence in the word face, the player's own doings in amber under their own section, impact lines indented. `WorldView` publishes numbers and rows instead of `%-9s`-padded strings.
  **The gate that should have existed.** `DataPageTest` walks all six pages on a world stepped to t=3,000 through the real loop and photographs them at 408 dp in both languages (plus the ⓘ card) — twelve stills under `build/reports/screens/`. Nothing in this repository had ever looked at these pages: the layout gate measures rows and never opens the screen, the boot gate opens the app and never walks the pages, the frame gate knows the pond and not a chart. Its first photograph convicted a bug no other gate could see — a bare `View` spacer in the odd half of the stat grid measured `WRAP_CONTENT` under an `AT_MOST` spec and took the whole column (`View.getDefaultSize` returns specSize for AT_MOST), pushing the species vitals and the footnote off the page. Also caught: the apex species has no reference band, so the hunter's row had silently vanished with the old Venator special case; it is back, with `level = -1` for "measured, not judged".
  Battery: `npm test` PASS (conformance bit-identical — nothing in the core was touched; app prose PASS, EN+DE), app suite 20/20 including the layout gate at **0 violations** with the new tab strip.
  One more lesson from the gate, on the merge into main: it passed alone and failed in the suite, because it founded its world BEFORE `buildActivity` and the shell's own boot (a front door that can adopt a saved experiment) runs in between — so whatever the suite had left behind decided what the gate photographed. It founds the world on the render thread AFTER the activity is up now, and asserts 120 samples before trusting the vitals. A test that assumes a starting state instead of making one is a test that passes for the wrong reason; the JNI core is one process-global singleton and the whole suite shares it.
  **Open, not fixed here (a real defect, found while reading the recorder):** `Sim::load` restores `w.flows` from the snapshot but never resets `obs`, so the recorder's `prev` still holds the pre-load world's cumulative counters. The first post-load sample of every DELTA channel (gpp, resp, mineralization, uptake, deaths) is therefore a lifetime total rather than a per-sample rate, and the ring still carries the previous world's history spliced onto the loaded one. This is the spike that dominates the owner's Umsatz screenshot. Note what the OBVIOUS fix does not do: `self.obs.reset()` alone sets `prev` to zero while `w.flows` holds the loaded world's lifetime totals, so the first sample would still be a lifetime total — `init_world` is only consistent because it zeroes BOTH. The recorder has to be reset AND its `prev` seeded from the restored flows. Carried over untouched besides: the ring's pre-load samples, `count`, the detector state and `sys_events`, so the charts splice two worlds and the narration can fire on the seam. A declared Rust change with its own `conform:core` recapture — deliberately not folded into a UI pass.
